# 운영 기준선 — 2026-07-14

## 측정 목적

무료 배포 환경에서 사용자가 처음 검색할 때 겪는 지연과 웜 상태 검색 시간을 분리했다. 아직 배포하지 않은 새 관측 코드의 성과가 아니라, 개선 전 공개 서비스의 기준선이다.

## 조건

- 시각: 2026-07-14 17:40 KST
- endpoint: `POST /api/policies/recommend`
- 입력: 개인정보가 없는 고정 질문 `월세 지원`, `age=null`, `k=5`
- Gemini 답변 생성은 포함하지 않음
- 첫 요청 1회와 연속 웜 요청 5회

## 결과

| 구분 | 요청 수 | 성공 | 결과 건수 | 응답시간 |
|---|---:|---:|---:|---:|
| 콜드 | 1 | 1/1 | 5 | 58,909ms |
| 웜 | 5 | 5/5 | 매회 5 | 평균 814.6ms |

웜 요청의 p50은 820ms, 관측 최대값은 850ms다. 첫 요청은 웜 평균보다 약 72배 느렸다.

## 해석 범위

- 검색 경로 자체는 웜 상태에서 1초 이내로 응답했다.
- 현재 측정만으로 API, ML 모델 로딩, DB 휴면 중 어느 구성요소가 58.9초를 차지했는지는 단정할 수 없다.
- 다음 배포에서 API와 ML 서비스의 readiness·구간별 타이머를 분리해 원인을 확인한다.
- 무료 예산을 유지해야 하므로 최소 인스턴스 상시 실행은 비용 확인 전 적용하지 않는다.

원본 측정값은 [baseline-2026-07-14.csv](baseline-2026-07-14.csv)에 보존한다.
