# Retrieval v3 D-081 dev-v2 generation-v9 strategy — launch-isolation pre-result freeze

Status: D-081 NEW LOGICAL STAGE (user-authorized continuation). Plaintext-free: SHAs, counts, timestamps, filenames, structural facts only — no query text, no labels, no id mappings.
Stage identity: D-081 creates a generation-v9 PRE-RESULT freeze ONLY. No source-truth snapshot, no candidate generation, no A/B/C, no selector in D-081.

## 1. Reconciled base (this record phase, actual repo/Git/remote/SSOT wins — observed)

- Branch `codex/retrieval-v3-user-search-quality` HEAD `06b554a9130c296fb9bb9210cb0a726516635d42` clean, local = upstream = direct remote identical.
- `git diff --check` PASS; `git diff HEAD -- ml-service/` 0; frozen six byte-identical by clean-tree inheritance from the D-079/D-080 verification (prereg `78420186…` + plan-v4 `a25d9c48…` + safe-action `c512fb56…` + policy-v2 `6fee9ec2…` + link-V2 `f028ce46…` + cost-V1 `5891b0ba…`; no tracked byte differs from verified remote HEAD).
- Audit `eval/retrieval-v3/audit/events.jsonl` 4 events SHA `90cfb54df614bc59e01551943436fdafc3cd5cac121b071742acbb2fb604c506` byte-identical (D-068 open grant/run untouched).
- Canonical v3 result absent; `eval/retrieval-v3/dev/` + `holdout/` absent; no dev-v2 branch/tag/worktree created or touched; protected v3 dev/holdout freeze branches pre-existing, untouched.
- Durable inputs read in this stage: D-079 strategy doc + `D-079` block, D-080 closure doc + `D-080` block, D-080-CORR doc + `D-080-CORR` block, frozen v8 `GENERATION_PLAN.json` (29447 bytes, SHA `2206582b58cdaac0f304662744c94e851359ba7bc62a567d7d25f0896f0f90c3`), `RUBRIC.json` (3330 bytes, SHA `43e57129fcef793bc6b185734680fd027b72bf1aa43c2df41e05ea5b56b8589f`), `PLAN_LOCK.json` (3483 bytes, SHA `0cad7acc8f986b30f33b074d7b0b0c31ac7f238631e650a5123021d3a17b5705`) — structure only, re-verified before use. Eight fingerprint files carried byte-identically (D070 `0acc6f27` 273, D071 `3a037d98` 273, D072 `ff3f65d6` 360, D074 `fde76331` 360, D076 `3feaab4d` 365/365, dev-v1 `57716c6a` 180q/228g, holdout `3463a8a1` 250q/212g, history `42e8534d` 248q/248g) plus `D076_CONTRACT_INVALID_SUMMARY.json` `0279c69a` (aggregate provenance reference). No old plaintext printed/stored outside the v8 builder; no D-074/D-076/D-078/D-080 labels/golds/ledgers/mappings/keymaps read. New v9 manifest `input/EXCLUSION_INPUTS.json` SHA `9f306fcb7a59820a7ad36c75ec83180336b4fadd005e593141e5a44aa9225d5b` (2111 bytes; EIGHT gates, required overlap 0). No failed-D078/D080 set (rows 0).
- Explicitly NOT read in this stage: v8 `source_truth.jsonl`, `source_truth_meta.json`, `anchors.json`, `anchors_1.json`, `anchors_2.json`, `candidates_merged.json`, author chunks, D-076/D-078/D-080 semantic rows, or any protected old plaintext. No `git cat-file` / `git show` / `checkout` / `restore` / sparse / worktree invocation in this stage (count 0).
- OMP `18.1.5`; root session model `opencode-go/muse-spark-1.3-contributor:xhigh` (runtime context; model config never changed); no project override (`opencode.json`/`.opencode.json` absent); exactly one neutral disposable smoke child spawned through the frozen helper before freeze (see §7), no other child agents.
- No protected plaintext recovery, no audit append/run/result/benchmark/retrieval/ranking/model/latency/HTTP, no candidate tuning/B, no ml-service change, no protected branch/tag/worktree/import, no history rewrite in this stage.

## 2. D-080-CORR phrase correction (append-only, originals verbatim)

- `docs/RETRIEVAL_V3_D080_PROVENANCE_CORRECTION.md` line 6 and the DECISIONS `D-080-CORR` block use the private phrase `Materialized before launch (user-authoritative)`.
- Append-only correction (this section supersedes ONLY that parenthetical): staging-before-launch was Web-observed filesystem/root-log evidence (two disjoint role-local roots, both `out/` empty), not a user assertion. The word `user-authoritative` is retired and MUST NOT be reused.
- Unchanged: D-080 + D-080-CORR text/history preserved verbatim; D-080 verdict CONTRACT_INVALID_GENERATION unchanged; valid author agents 0; candidate/query rows 0.

## 3. Standing (unchanged)

- D-080 disposition is CONTRACT_INVALID_GENERATION per D-080 + D-080-CORR (as corrected above). Its source snapshot (13589 rows) + mechanical slot anchors are failed diagnostic evidence only and NEVER reusable/relabelable/supplemented. D-080 selector counts do not exist (candidate/query rows 0) and MUST NOT tune quotas/reserve.
- D-078 disposition is CONTRACT_INVALID_GENERATION per D-078 + D-078-CORR. Its source snapshot + mechanical slot anchors are failed diagnostic evidence only and NEVER reusable/relabelable/supplemented. D-078 selector counts do not exist (candidate/query rows 0) and MUST NOT tune quotas/reserve.
- D-076 disposition is CONTRACT_INVALID_GENERATION per D-076 + D-076-CORR. Its 360 rows + 5 coordinator-overwritten blocks are failed diagnostic evidence only and NEVER reusable/relabelable/supplemented. The 365 fingerprints are failure-exclusion evidence only. D-076 selector counts MUST NOT tune quotas/reserve.
- D-074 canonical disposition is CONTRACT_INVALID_GENERATION per D-074-SC. Its 360/A/B/C/selector are failed diagnostic evidence only and NEVER reusable/relabelable/supplemented. D-074 selector counts MUST NOT tune quotas/reserve.
- The D-073 paired-grounded ambiguous authoring contract was frozen pre-D074 and remains the intended authoring contract; kept unchanged.
- Final evaluation contract and 18 candidate configs/gates unchanged.

## 4. V9 identity and unchanged final contract

- New private builder only: `C:\Users\joji\Documents\programming\bc-v3-dev-v2-builder-20260905-v9`. Never D-070/D-071/D-072/v5/v6/v6r1/v7/v8 directories.
- New identity: plan_version `retrieval-v3-dev-generation-v9`; seed `benefit-compass-retrieval-v3-dev-v2-generation-v9-2026-09-05` (new builder only); candidate IDs `v3g9-001..v3g9-360`.
- Final contract EXACTLY unchanged from v8: total 180; strata 21/25/21/25/18/20/23/27; headline first-six 130; safety ambiguous 23 + unsupported 27 = 50; location exact 54 at 6/7/6/8/5/6/7/9. 18 candidate configs/gates unchanged.
- Reserve remains uniform 2.0x, exactly 360 slots 42/50/42/50/36/40/46/54 and location 108 at 12/14/12/16/10/12/14/18. Do NOT inflate or target reserve from D-074/D-076/D-078/D-080 diagnostics. Symmetric construction capacity only; no gate change.
- NO target/reserve change from D074/D076/D078/D080 diagnostics. No outcome-driven tuning. No failed-D080 query set (rows 0).

## 5. V9 authoring, annotation, rubric, selector (semantics identical to v8; launch-isolation mechanics repair only)

- All eight authoring contracts byte-semantically identical to v8, including paired-grounded ambiguous (two distinct snapshot anchors, one omitted dimension, shared frame, two completions plus distinguishing evidence; location-bearing ambiguous shares explicit location with non-location omitted dimension). Verified: `authoring_contracts` object identical. No semantic challenger/prefilter.
- Neutral rubric semantic definitions byte-semantically identical to v8 (`dimensions` + `judgment_rules` + `role` identical; diff keys are `plan_version` + `rubric_version` only, now `retrieval-v3-dev-generation-v9-rubric-v1`). No quotas/intended labels in rubric.
- A/B genuine independent ALL 360, C EVERY 360, agreement method (query_dim_any/gold_any/full_agreement plus task-local per-gold union+ABSENT diagnostic) and exact lexicographically-smallest selector algorithm identical to v8 (verified identical; selector `constraints` identical including 8-set query gate `failed-D076-365`; gold overlap stays dev-v1/holdout/history-only).
- Query overlap 0 vs dev-v1/holdout/history + failed D070/D071/D072/D074/D076 (EIGHT sets); gold overlap 0 vs canonical dev-v1/holdout/history only. Selected-only global gold uniqueness. Source snapshot taken only in later Phase C after Web/user continuation. Plan/rubric/lock immutable after freeze; infeasible/contract-invalid => STOP, no supplement/relabel/retune.
- Launch-isolation repair ONLY (no semantic change): all five launch roles (exactly two authors, reviewer A, reviewer B, C) use ONLY the frozen `launch_top_level_paseo.py` (PASEO_CLI captured before sanitization, ALL PASEO_/OMP_ env removed, `paseo run --background --cwd` staging `--provider omp/opencode-go/muse-spark-1.3-contributor --thinking xhigh --mode full`, agent-ID parse, hard inspect verification ParentAgentId null + cwd exact + model + fallback false); runtime mechanics carry no literal plan SHA; `run_selector` hashes actual `GENERATION_PLAN.json` and asserts `PLAN_LOCK.json` binding at runtime; generation-v9 neutral artifact labels/filenames; no stale seal labels; C opaque IDs `v9c-001..360` consistently with keymap-derived split and no hardcoded old prefix; briefs/headers generation-v9 neutral (old builders named only as forbidden examples, never as active paths).

## 6. V9 author filesystem-confinement contract (isolation mechanics repaired to helper-only launch; confinement/no-pre-keymap identical to v8)

Durable claim is `two disjoint author staging roots + verified session cwd confinement + frozen mechanics + coordinator-never-authors + same-author repair + pre-A/B hard transcript audit`, not stronger. No OS-sandbox claim.

1) BEFORE authors start, coordinator freezes + SHA-pins all author helper/validator/merge/launcher mechanics (see §8): `launch_top_level_paseo.py`, `author_brief.md`, `search_snapshot.py`, `check_anchor.py` (EIGHT sets), `validate_pool.py` (candidate IDs + frozen fail codes only), `merge_chunks.py`, `make_anchors.py`, packet/agreement/C/selector scripts. No validator/helper/merge/launcher patching after authoring starts.
2) Coordinator materializes TWO independent random-UUID author staging roots under a neutral private temp parent outside the builder. Author-1 root: role-specific 180-slot packet (positions 1..180) + `source_truth.jsonl` copy + `RUBRIC.json` + author brief + search/check helper + fingerprint-only exclusions + own `out/`. Author-2 root: positions 181..360 + same role-local set + own `out/`. No counterpart packet/output, no keymap/candidates/plan/ledgers, no coordinator files. Prompts contain only role + own staging-root path/local filenames — never builder/repo/other-author/old-builder paths.
3) Authors MUST be launched ONLY via frozen `launch_top_level_paseo.py` as genuine top-level Paseo agents (exactly TWO, no generic OMP `task`-child sharding, no subagents, no descendants). Coordinator records each author Paseo agent ID + hard inspect verification showing ParentAgentId null + ACTUAL cwd == staging root + model `muse-spark-1.3-contributor` + fallback false; mismatch => CONTRACT_INVALID_GENERATION.
4) Mechanical validation (frozen `validate_pool.py`) may ONLY emit candidate IDs + frozen fail codes. Coordinator MUST NEVER author or rewrite `query_text`/ledger semantic content and MUST NEVER patch validators based on failures after authoring starts. Repairs go via `paseo send` to the SAME top-level author agent with IDs + codes only and no suggested replacement text; only that author edits its own output.
5) Coordinator may mechanically copy/hash/merge frozen author bytes after PASS (frozen `merge_chunks.py`) — no semantic edit, no relabeling.
6) BEFORE A/B, hard transcript audit: exactly 2 author agents with no descendants; cwd confinement + effective-path PASS for every author tool call (explicit cwd inside root; relative resolves against pinned root cwd; absolute stays under root; no `..` escape, no parent/system/OMP-session/counterpart/builder/old-builder access); coordinator transcript contains no semantic per-row replacement/write to author outputs. Violation => CONTRACT_INVALID_GENERATION.
7) Only after the author audit PASSES may coordinator proceed to A/B staging. Reviewer A, reviewer B, and C each launch ONLY via the SAME frozen helper with their own random staging roots and identical hard inspect verification. Reviewer/C independent `--cwd` confinement / no-pre-review-keymap / effective-path gates retained unchanged from v8 (plan `reviewer_filesystem_confinement` object identical except helper-only launch wording).

## 7. Neutral disposable smoke verification BEFORE freeze (actual, observed, plaintext-free)

- From THIS agent-scoped root through the frozen helper, one neutral disposable smoke child launched into a temp directory with NO repo/source/project files: `C:/Users/joji/Documents/programming/bc-v9-smoke-20260905-neutral` (empty at launch).
- Child prompt reported pwd only; no repo/source/project file access.
- Returned smoke agent ID `c30ef544-a4d0-47ca-9b82-56a3de7cc24a`; hard inspect verification PASS: ParentAgentId null, actual Cwd exactly equal to requested staging, Model `opencode-go/muse-spark-1.3-contributor`, Provider `omp`, fallback false (no fallback keys, model suffix match).
- Child observed idle after report; `paseo stop` issued (no-op INTERRUPTED, already idle). No author/reviewer/C role, no candidate/benchmark/protected action.
- Smoke proves the helper produces genuine top-level cwd-bound agents, directly addressing the D-080 failure mode (both authors cwd = main repo, both ParentAgentId = D-080 root).

## 8. Plan lock truthful flags

- `source_truth_content_read_for_this_plan=false`; `v6_source_truth_content_read=false`; `v6r1_source_truth_content_read_in_this_stage=false`; `v7_source_truth_content_read_in_this_stage=false`; `v8_source_truth_content_read_in_this_stage=false` (v8 source truth was taken only inside the failed D-080 execution, never in this freeze).
- `d070/d071/d072/d074/d076/d078/d080_semantic_rows_read_for_plan=false` (D-076 inputs used: fingerprint-file SHA + count/unique 365 + closure-summary aggregate SHAs/counts only; D-078/D-080 rows 0 so no fingerprint set; no `source_truth`/`candidates`/`anchors` bytes read).
- `protected_old_plaintext_read=false`; `git_object_scan_cat-file_show_checkout_restore_sparse_worktree=0`.
- `d074_d076_fingerprint_inputs_carried_byte_identical_from_v8=true`; v9 exclusion manifest SHA `9f306fcb7a59820a7ad36c75ec83180336b4fadd005e593141e5a44aa9225d5b`; `author_mechanics_frozen_before_authors=true` with per-file SHAs in lock.
- `smoke_top_level_verification` pinned in lock (agent ID + parent-null/cwd-exact/model/fallback, pwd-only neutral prompt).

## 9. Phase B freeze record (frozen BEFORE source-truth content; immutable afterward)

- Builder: `C:\Users\joji\Documents\programming\bc-v3-dev-v2-builder-20260905-v9` (new directory; never D-070/D-071/D-072/v5/v6/v6r1/v7/v8).
- Exclusions (`carry_exclusions_v9.py`): 8 fingerprint files carried byte-identically from v8 `input/` — failed_d070 `0acc6f27` (273), failed_d071 `3a037d98` (273), failed_d072 `ff3f65d6` (360), failed_d074 `fde76331` (360), failed_d076 `3feaab4d` (365/365), dev-v1 `57716c6a` (180q/228g), holdout `3463a8a1` (250q/212g), history `42e8534d` (248q/248g) — plus carried `D076_CONTRACT_INVALID_SUMMARY.json` `0279c69a` (aggregate provenance reference). No old plaintext printed/stored outside the v8 builder; no D-074/D-076/D-078/D-080 labels/golds/ledgers/mappings/keymaps read. New v9 manifest `input/EXCLUSION_INPUTS.json` SHA `9f306fcb7a59820a7ad36c75ec83180336b4fadd005e593141e5a44aa9225d5b` (2111 bytes; EIGHT gates, required overlap 0). No failed-D078/D080 set (rows 0).
- Frozen mechanics (SHA-pinned in plan `author_isolation.mechanics_shas` + lock, all inside the v9 builder): `search_snapshot.py` 1706 bytes `d5366f6c…`, `check_anchor.py` 3025 `3f52fe83…` (EIGHT sets), `validate_pool.py` 13980 `c56affc9…` (EIGHT-set gate; fail records are `{candidate_id, code}` only; IDs `v3g9-001..360`), `merge_chunks.py` 1457 `52edd1fb…`, `make_anchors.py` 2137 `15580287…` (slots `v3g9-001..360`, Author-1 1..180 / Author-2 181..360), `build_packets_ab.py` 3458 `408d3650…`, `build_packets_c.py` 3206 `76085ea4…` (`v9c-` consistently), `merge_raw_ab.py` 2206 `19864c24…`, `build_agreement.py` 6158 `473cf91b…` (generation-v9 agreement description, no stale seal label), `merge_c.py` 4935 `22a5bbe7…` (`v9c-` + keymap-derived split, no hardcoded old prefix), `reconstruct_keymaps.py` 2744 `d129ad82…`, `run_selector.py` 17114 `0bf6d863…` (no-literal-SHA + runtime binding; `GENERATION_V9_HOLD_SUMMARY`), `take_snapshot.py` 3362 `bdb04ef1…`, `author_brief.md` 9366 `dde54327…` (v8 added to forbidden examples), `reviewer_brief.md` 4364 `9cb7fc5c…`, `c_brief.md` 3154 `77d174eb…` (`v9c-`), `carry_exclusions_v9.py` 2487 `6c9cd361…`, `freeze_plan_v9.py` 3262 `bffa2ad8…`, `launch_top_level_paseo.py` 7588 `f91355c7…` (PASEO_CLI-before-sanitization + ALL PASEO_/OMP_ removed + background/cwd/provider/thinking/mode + ID parse + inspect verify).
- Frozen (canonical single-line bytes + LF): `GENERATION_PLAN.json` 31522 bytes SHA `386bc79a858b9a9fcaf40a7728291c2370b1f97e90a56395266c67f2f03e9e54` (version `retrieval-v3-dev-generation-v9`, seed `benefit-compass-retrieval-v3-dev-v2-generation-v9-2026-09-05`, candidate IDs `v3g9-001..360`, reserve 42/50/42/50/36/40/46/54=360 + location 12/14/12/16/10/12/14/18=108, paired-grounded ambiguous contract unchanged, seven other contracts unchanged, A/B-all-360 + C-every-360, agreement scope + task-local per-gold method unchanged, exact lexicographically-smallest selector with 8-set query gate unchanged, author-isolation contract with v9 mechanics SHAs + helper-only launch, reviewer/C confinement identical except helper-only launch wording, keymap reconstruction unchanged, `stage_forbidden` generation-v9 with explicit git-object-recovery forbid + v8 old-builder forbid).
- Frozen (canonical single-line bytes + LF): `RUBRIC.json` 3330 bytes SHA `728fdb9deb06915f17cf0c21f5efca9d29b0ca8b3532b4c884675b9026da63e1` (dimensions/judgment-rules/role identical to v8; diff keys are `plan_version` + `rubric_version` only, now `retrieval-v3-dev-generation-v9-rubric-v1`).
- Frozen (canonical single-line bytes + LF): `PLAN_LOCK.json` 3947 bytes SHA `c27cfef04bb4cf40d4f14d843f05d893778a26df910a0ec37a411812ace47d33` (source_truth false, v6/v6r1/v7/v8-not-read-in-this-stage, d070/d071/d072/d074/d076/d078/d080 semantic false, protected_old false, git-object-scan 0, D074/D076 carried from v8 recorded, mechanics SHA-pinned incl launch helper, `supersedes_v8_plan` `2206582b…`, smoke verification pinned, `frozen_at` `2026-09-05T07:31:53+00:00` truthful observed, no placeholder).
- Provenance (actual, observed): OMP `18.1.5`; root session model `opencode-go/muse-spark-1.3-contributor:xhigh` (runtime context; model config never changed); no project model override (no `opencode.json`/`.opencode.json`); one neutral smoke child through the helper before freeze (see §7); no fallback model used. Phase C author sessions will be recorded with their model/session evidence at spawn time.
- Static verification before source truth (observed): all 16 scripts + carry + freeze + launch compile; runtime binding check hashes actual plan `386bc79a…` and asserts lock/rubric binding consistent; final/reserve/authoring/rubric/A/B/C/agreement/selector identical to v8 verified; 8 exclusion sets byte-identical verified; `v3g8-` 0, `v8c-` 0, `PLAN_SHA` literal 0, `D076_HOLD_SUMMARY` 0, `generation-v8` active 0, old builder active paths 0 (forbidden examples + one-time carry source only); `source_truth.jsonl`/`candidates`/`anchors`/`sealed`/staging absent in v9 builder.
- Gate: this record is committed+pushed and remote-verified BEFORE any Phase C source-truth content read. Post-freeze rule in force: NEVER mutate plan/rubric/lock; infeasible/contract-invalid -> STOP/HOLD, no supplement/relabel/retune/recycle.

## 10. End state and STOP

- Main contains only plaintext-free D-081 strategy/freeze records. Private v9 builder contains exclusions + scripts + plan/rubric/lock only; `source_truth.jsonl` ABSENT; candidates ABSENT; author/reviewer staging roots NOT created yet (smoke temp dir is neutral, outside builder, holds no source/candidate content).
- Verify clean local=upstream=direct remote, diff-check PASS, ml-service 0, frozen six/audit exact, v6/v6r1/v7/v8 bytes preserved, result/dev/holdout absent, no dev-v2 protected refs.
- STOP for Web independent review. Phase C generation requires Web review/user continuation and is NOT authorized in D-081. No D-082 execution.

## 11. Forbidden counts (this stage)

Forbidden counts all 0: source-truth snapshot, candidate generation, A/B/C, selector, benchmark/retrieval/ranking/latency/HTTP/model-encode, D068 retry/audit append/result, protected plaintext/recovery, `git cat-file` / `git show` / `checkout` / `restore` / sparse / worktree protected-data/object scanning, protected branch/tag/worktree/import, ml-service change, history rewrite, D074/D076/D078/D080 row reuse (fingerprint-only exclusion excepted), D-081/v9 repair beyond this freeze, D-082 execution.
