# Retrieval v3 D-079 dev-v2 generation-v8 strategy — provenance-repair pre-result freeze

Status: D-079 NEW LOGICAL STAGE (user-authorized continuation). Plaintext-free: SHAs, counts, timestamps, filenames, structural facts only — no query text, no labels, no id mappings.
Stage identity: D-079 creates a generation-v8 PRE-RESULT freeze ONLY. No source-truth snapshot, no candidate generation, no A/B/C, no selector in D-079.

## 1. Reconciled base (this record phase, actual repo/Git/remote/SSOT wins — observed)

- Branch `codex/retrieval-v3-user-search-quality` HEAD `f79f3b882febd24ead361ec141322792da59e9d0` clean, local = upstream = direct remote identical.
- `git diff --check` PASS; `git diff HEAD -- ml-service/` 0; frozen six byte-identical by clean-tree inheritance from the D-077/D-078 verification (prereg `78420186…` + plan-v4 `a25d9c48…` + safe-action `c512fb56…` + policy-v2 `6fee9ec2…` + link-V2 `f028ce46…` + cost-V1 `5891b0ba…`; no tracked byte differs from verified remote HEAD).
- Audit `eval/retrieval-v3/audit/events.jsonl` 4 events SHA `90cfb54df614bc59e01551943436fdafc3cd5cac121b071742acbb2fb604c506` byte-identical (D-068 open grant/run untouched).
- Canonical v3 result absent; `eval/retrieval-v3/dev/` + `holdout/` absent; no dev-v2 branch/tag/worktree created or touched; protected v3 dev/holdout freeze branches pre-existing, untouched.
- Durable inputs read in this stage: D-078 closure doc + `D-078` block, D-078-CORR doc + `D-078-CORR` block, D-077 strategy doc + `D-077` block, frozen v7 `GENERATION_PLAN.json` (28766 bytes, SHA `c8adfcf2c1cb86e622c7fa4033b7dd72b63de42494de332261f3a489c770d1da`), `RUBRIC.json` (3330 bytes, SHA `597aa53a39c69cfb9409a339ebe8f79f15e07f635899d4d24203289ea46de492`), `PLAN_LOCK.json` (3332 bytes, SHA `1db0687e0eb5321a64236991139144440e2f34678b91e91bcce80103afd5d140`) — structure only, re-verified before use. Eight fingerprint files carried byte-identically (D070 `0acc6f27` 273, D071 `3a037d98` 273, D072 `ff3f65d6` 360, D074 `fde76331` 360, D076 `3feaab4d` 365/365, dev-v1 `57716c6a` 180q/228g, holdout `3463a8a1` 250q/212g, history `42e8534d` 248q/248g) plus `D076_CONTRACT_INVALID_SUMMARY.json` `0279c69a` (aggregate provenance reference). No failed-D078 query set exists because D078 candidate/query rows were 0.
- Explicitly NOT read in this stage: v7 `source_truth.jsonl`, `source_truth_meta.json`, `anchors.json`, `anchors_1.json`, `anchors_2.json`, `candidates_merged.json`, author chunks, D-076/D-078 semantic rows, or any protected old plaintext. No `git cat-file` / `git show` / `checkout` / `restore` / sparse / worktree invocation in this stage (count 0).
- OMP `18.1.5`; root session model `opencode-go/muse-spark-1.3-contributor:xhigh` (runtime context; model config never changed); no project override (`opencode.json`/`.opencode.json` absent); no child agents spawned through freeze.
- No protected plaintext recovery, no audit append/run/result/benchmark/retrieval/ranking/model/latency/HTTP, no candidate tuning/B, no ml-service change, no protected branch/tag/worktree/import, no history rewrite in this stage.

## 2. Standing (unchanged)

- D-078 disposition is CONTRACT_INVALID_GENERATION per D-078 + D-078-CORR. Its source snapshot + mechanical slot anchors are failed diagnostic evidence only and NEVER reusable/relabelable/supplemented. D-078 selector counts do not exist (candidate/query rows 0) and MUST NOT tune quotas/reserve.
- D-076 disposition is CONTRACT_INVALID_GENERATION per D-076 + D-076-CORR. Its 360 rows + 5 coordinator-overwritten blocks are failed diagnostic evidence only and NEVER reusable/relabelable/supplemented. The 365 fingerprints are failure-exclusion evidence only. D-076 selector counts MUST NOT tune quotas/reserve.
- D-074 canonical disposition is CONTRACT_INVALID_GENERATION per D-074-SC. Its 360/A/B/C/selector are failed diagnostic evidence only and NEVER reusable/relabelable/supplemented. D-074 selector counts MUST NOT tune quotas/reserve.
- The D-073 paired-grounded ambiguous authoring contract was frozen pre-D074 and remains the intended authoring contract; kept unchanged.
- Final evaluation contract and 18 candidate configs/gates unchanged.

## 3. V8 identity and unchanged final contract

- New private builder only: `C:\Users\joji\Documents\programming\bc-v3-dev-v2-builder-20260905-v8`. Never D-070/D-071/D-072/v5/v6/v6r1/v7 directories.
- New identity: plan_version `retrieval-v3-dev-generation-v8`; seed `benefit-compass-retrieval-v3-dev-v2-generation-v8-2026-09-05` (new builder only); candidate IDs `v3g8-001..v3g8-360`.
- Final contract EXACTLY unchanged from v7: total 180; strata 21/25/21/25/18/20/23/27; headline first-six 130; safety ambiguous 23 + unsupported 27 = 50; location exact 54 at 6/7/6/8/5/6/7/9. 18 candidate configs/gates unchanged.
- Reserve remains uniform 2.0x, exactly 360 slots 42/50/42/50/36/40/46/54 and location 108 at 12/14/12/16/10/12/14/18. Do NOT inflate or target reserve from D-074/D-076/D-078 diagnostics. Symmetric construction capacity only; no gate change.
- NO target/reserve change from D074/D076/D078 diagnostics. No outcome-driven tuning. No failed-D078 query set (rows 0).

## 4. V8 authoring, annotation, rubric, selector (semantics identical to v7; provenance/mechanics repair only)

- All eight authoring contracts byte-semantically identical to v7, including paired-grounded ambiguous (two distinct snapshot anchors, one omitted dimension, shared frame, two completions plus distinguishing evidence; location-bearing ambiguous shares explicit location with non-location omitted dimension). Verified: `authoring_contracts` object identical. No semantic challenger/prefilter.
- Neutral rubric semantic definitions byte-semantically identical to v7 (`dimensions` + `judgment_rules` + `role` identical; diff keys are `plan_version` + `rubric_version` only, now `retrieval-v3-dev-generation-v8-rubric-v1`). No quotas/intended labels in rubric.
- A/B genuine independent ALL 360, C EVERY 360, agreement method (query_dim_any/gold_any/full_agreement plus task-local per-gold union+ABSENT diagnostic) and exact lexicographically-smallest selector algorithm identical to v7 (verified identical; selector `constraints` identical including 8-set query gate `failed-D076-365`; gold overlap stays dev-v1/holdout/history-only).
- Query overlap 0 vs dev-v1/holdout/history + failed D070/D071/D072/D074/D076 (EIGHT sets); gold overlap 0 vs canonical dev-v1/holdout/history only. Selected-only global gold uniqueness. Source snapshot taken only in later Phase C after Web/user continuation. Plan/rubric/lock immutable after freeze; infeasible/contract-invalid => STOP, no supplement/relabel/retune.
- Provenance repair ONLY (no semantic change): runtime mechanics carry no literal plan SHA; `run_selector` hashes actual `GENERATION_PLAN.json` and asserts `PLAN_LOCK.json` binding at runtime; generation-v8 neutral artifact labels/filenames; no stale seal labels; C opaque IDs `v8c-001..360` consistently with keymap-derived split and no hardcoded old prefix; briefs/headers generation-v8 neutral (old builders named only as forbidden examples, never as active paths).

## 5. V8 author filesystem-confinement contract (isolation mechanics identical to v7; provenance labels repaired)

Durable claim is `two disjoint author staging roots + verified session cwd confinement + frozen mechanics + coordinator-never-authors + same-author repair + pre-A/B hard transcript audit`, not stronger. No OS-sandbox claim.

1) BEFORE authors start, coordinator freezes + SHA-pins all author helper/validator/merge mechanics (see §7): `author_brief.md`, `search_snapshot.py`, `check_anchor.py` (EIGHT sets), `validate_pool.py` (candidate IDs + frozen fail codes only), `merge_chunks.py`, `make_anchors.py`, packet/agreement/C/selector scripts. No validator/helper/merge patching after authoring starts.
2) Coordinator materializes TWO independent random-UUID author staging roots under a neutral private temp parent outside the builder. Author-1 root: role-specific 180-slot packet (positions 1..180) + `source_truth.jsonl` copy + `RUBRIC.json` + author brief + search/check helper + fingerprint-only exclusions + own `out/`. Author-2 root: positions 181..360 + same role-local set + own `out/`. No counterpart packet/output, no keymap/candidates/plan/ledgers, no coordinator files. Prompts contain only role + own staging-root path/local filenames — never builder/repo/other-author/old-builder paths.
3) Authors MUST be launched as genuine top-level Paseo agents (exactly TWO, no generic OMP `task`-child sharding, no subagents, no descendants) via non-GUI CLI `paseo run --cwd <that author's staging root> --provider omp/opencode-go/muse-spark-1.3-contributor --thinking xhigh --mode full` (or byte-equivalent). Coordinator records each author Paseo agent ID + inspect/session evidence showing ACTUAL cwd == staging root, model, fallback flag; mismatch => CONTRACT_INVALID_GENERATION.
4) Mechanical validation (frozen `validate_pool.py`) may ONLY emit candidate IDs + frozen fail codes. Coordinator MUST NEVER author or rewrite `query_text`/ledger semantic content and MUST NEVER patch validators based on failures after authoring starts. Repairs go via `paseo send` to the SAME top-level author agent with IDs + codes only and no suggested replacement text; only that author edits its own output.
5) Coordinator may mechanically copy/hash/merge frozen author bytes after PASS (frozen `merge_chunks.py`) — no semantic edit, no relabeling.
6) BEFORE A/B, hard transcript audit: exactly 2 author agents with no descendants; cwd confinement + effective-path PASS for every author tool call (explicit cwd inside root; relative resolves against pinned root cwd; absolute stays under root; no `..` escape, no parent/system/OMP-session/counterpart/builder/old-builder access); coordinator transcript contains no semantic per-row replacement/write to author outputs. Violation => CONTRACT_INVALID_GENERATION.
7) Only after the author audit PASSES may coordinator proceed to A/B staging. Reviewer/C independent `--cwd` confinement / no-pre-review-keymap / effective-path gates retained unchanged from v7 (plan `reviewer_filesystem_confinement` object identical).

## 6. Plan lock truthful flags

- `source_truth_content_read_for_this_plan=false`; `v6_source_truth_content_read=false`; `v6r1_source_truth_content_read_in_this_stage=false`; `v7_source_truth_content_read_in_this_stage=false` (v7 source truth was taken only inside the failed D-078 execution, never in this freeze).
- `d070/d071/d072/d074/d076/d078_semantic_rows_read_for_plan=false` (D-076 inputs used: fingerprint-file SHA + count/unique 365 + closure-summary aggregate SHAs/counts only; D-078 rows 0 so no fingerprint set; no `source_truth`/`candidates`/`anchors` bytes read).
- `protected_old_plaintext_read=false`; `git_object_scan_cat-file_show_checkout_restore_sparse_worktree=0`.
- `d074_d076_fingerprint_inputs_carried_byte_identical_from_v7=true`; v8 exclusion manifest SHA `e8a8e957a5abb8f40704a5ee3fb371defba9a438cbb765a3bc52b9c813b36900`; `author_mechanics_frozen_before_authors=true` with per-file SHAs in lock.

## 7. Phase B freeze record (frozen BEFORE source-truth content; immutable afterward)

- Builder: `C:\Users\joji\Documents\programming\bc-v3-dev-v2-builder-20260905-v8` (new directory; never D-070/D-071/D-072/v5/v6/v6r1/v7).
- Exclusions (`carry_exclusions_v8.py`): 8 fingerprint files carried byte-identically from v7 `input/` — failed_d070 `0acc6f27` (273), failed_d071 `3a037d98` (273), failed_d072 `ff3f65d6` (360), failed_d074 `fde76331` (360), failed_d076 `3feaab4d` (365/365), dev-v1 `57716c6a` (180q/228g), holdout `3463a8a1` (250q/212g), history `42e8534d` (248q/248g) — plus carried `D076_CONTRACT_INVALID_SUMMARY.json` `0279c69a` (aggregate provenance reference). No old plaintext printed/stored outside the v7 builder; no D-074/D-076/D-078 labels/golds/ledgers/mappings/keymaps read. New v8 manifest `input/EXCLUSION_INPUTS.json` SHA `e8a8e957a5abb8f40704a5ee3fb371defba9a438cbb765a3bc52b9c813b36900` (2098 bytes; EIGHT gates, required overlap 0). No failed-D078 set (rows 0).
- Frozen mechanics (SHA-pinned in plan `author_isolation.mechanics_shas` + lock, all inside the v8 builder): `search_snapshot.py` 1706 bytes `71eb653a…`, `check_anchor.py` 3025 `6f8c3a5d…` (EIGHT sets), `validate_pool.py` 13980 `9fb7d8a5…` (EIGHT-set gate; fail records are `{candidate_id, code}` only), `merge_chunks.py` 1457 `4906daf6…`, `make_anchors.py` 2137 `e9afcdd9…` (slots `v3g8-001..360`, Author-1 1..180 / Author-2 181..360), `build_packets_ab.py` 3458 `ac163a6d…`, `build_packets_c.py` 3206 `e4f569b1…` (`v8c-` consistently), `merge_raw_ab.py` 2206 `14534c77…`, `build_agreement.py` 6158 `aa91d440…` (generation-v8 agreement description, no stale seal label), `merge_c.py` 4935 `aa62258f…` (`v8c-` + keymap-derived split, no hardcoded old prefix), `reconstruct_keymaps.py` 2744 `7d9d40c6…`, `run_selector.py` 17114 `b896f09e…` (NO literal plan SHA; runtime plan-hash + lock assert; generation-v8 neutral HOLD/sealed/provenance filenames and descriptions), `take_snapshot.py` 3362 `998cadce…`, `author_brief.md` 9330 `3f87850b…` (generation-v8 header, `v3g8-` slots, old builders as forbidden examples only), `reviewer_brief.md` 4364 `e59ba575…` (generation-v8 header), `c_brief.md` 3154 `e198ea5f…` (`v8c-` consistently), `carry_exclusions_v8.py` 2477 `7236ee01…`, `freeze_plan_v8.py` 2843 `7bd83570…`.
- Frozen (canonical single-line bytes + LF): `GENERATION_PLAN.json` 29447 bytes SHA `2206582b58cdaac0f304662744c94e851359ba7bc62a567d7d25f0896f0f90c3` (version `retrieval-v3-dev-generation-v8`, seed `benefit-compass-retrieval-v3-dev-v2-generation-v8-2026-09-05`, candidate IDs `v3g8-001..360`, reserve 42/50/42/50/36/40/46/54=360 + location 12/14/12/16/10/12/14/18=108, paired-grounded ambiguous contract unchanged, seven other contracts unchanged, A/B-all-360 + C-every-360, agreement scope + task-local per-gold method unchanged, exact lexicographically-smallest selector with 8-set query gate unchanged, author-isolation contract with v8 mechanics SHAs, reviewer/C confinement identical to v7, keymap reconstruction unchanged, `stage_forbidden` generation-v8 with explicit git-object-scan forbid).
- Frozen (canonical single-line bytes + LF): `RUBRIC.json` 3330 bytes SHA `43e57129fcef793bc6b185734680fd027b72bf1aa43c2df41e05ea5b56b8589f` (dimensions/judgment-rules/role identical to v7; diff keys are `plan_version` + `rubric_version` only, now `retrieval-v3-dev-generation-v8-rubric-v1`).
- Frozen (canonical single-line bytes + LF): `PLAN_LOCK.json` 3483 bytes SHA `0cad7acc8f986b30f33b074d7b0b0c31ac7f238631e650a5123021d3a17b5705` (source_truth false, v6/v6r1/v7-not-read-in-this-stage, d070/d071/d072/d074/d076/d078 semantic false, protected_old false, git-object-scan 0, D074/D076 carried from v7 recorded, mechanics SHA-pinned, `supersedes_v7_plan` `c8adfcf2…`, `frozen_at` `2026-09-05T07:00:29+00:00` truthful observed, no placeholder).
- Provenance (actual, observed): OMP `18.1.5`; root session model `opencode-go/muse-spark-1.3-contributor:xhigh` (runtime context; model config never changed); no project model override (no `opencode.json`/`.opencode.json`); no child agents spawned through freeze; no fallback model used. Phase C author sessions will be recorded with their model/session evidence at spawn time.
- Static verification before source truth (observed): all 18 mechanics compile; runtime binding check hashes actual plan `2206582b…` and asserts lock/rubric binding consistent; final/reserve/authoring/rubric/A/B/C/agreement/selector identical to v7 verified; 8 exclusion sets byte-identical verified; `v3g7-` 0, `v6r1c-` 0, `PLAN_SHA` literal 0, `D076_HOLD_SUMMARY` 0, `D-077`/`D077` 0, `D-074 seal` 0, `generation-v7` active 0 (historical carry/freeze/manifest references only), old builder active paths 0 (forbidden examples + one-time carry source only); `source_truth.jsonl`/`candidates`/`anchors`/`sealed`/staging absent in v8 builder.
- Gate: this record is committed+pushed and remote-verified BEFORE any Phase C source-truth content read. Post-freeze rule in force: NEVER mutate plan/rubric/lock; infeasible/contract-invalid -> STOP/HOLD, no supplement/relabel/retune/recycle.

## 8. End state and STOP

- Main contains only plaintext-free D-079 strategy/freeze records. Private v8 builder contains exclusions + scripts + plan/rubric/lock only; `source_truth.jsonl` ABSENT; candidates ABSENT; author/reviewer staging roots NOT created yet.
- Verify clean local=upstream=direct remote, diff-check PASS, ml-service 0, frozen six/audit exact, v6/v6r1/v7 bytes preserved, result/dev/holdout absent, no dev-v2 protected refs.
- STOP for Web independent review. Phase C generation requires Web review/user continuation and is NOT authorized in D-079. No D-080 execution.

## 9. Forbidden counts (this stage)

Forbidden counts all 0: source-truth snapshot, candidate generation, A/B/C, selector, benchmark/retrieval/ranking/latency/HTTP/model-encode, D068 retry/audit append/result, protected plaintext/recovery, `git cat-file` / `git show` / `checkout` / `restore` / sparse / worktree protected-data/object scanning, protected branch/tag/worktree/import, ml-service change, history rewrite, D074/D076/D078 row reuse (fingerprint-only exclusion excepted), D-079/v8 repair beyond this freeze, D-080 execution.
