# Retrieval v3 D-081-SC2 generation-v9r2 correction — exact-directory first-3-lines narrow repair

Status: SAME-STAGE second Web-HOLD → v9r2 PRE-RESULT narrow repair ONLY (user-authorized continuation). Plaintext-free: SHAs, counts, timestamps, filenames, structural facts only — no query text, no labels, no id mappings. D-081/v9/v9r1 records preserved verbatim; this doc + DECISIONS `D-081-SC2` block + SESSION-LOG entry are the only repo additions.

## 1. Reconciled base (actual repo/remote wins, this session — observed)

- Branch `codex/retrieval-v3-user-search-quality` HEAD `44c848534726c679a34b651724eaea493dc7a1c3` clean, local = upstream = direct remote identical (`https://github.com/crushonyou2/benefit-compass.git`).
- `git status --porcelain` clean; `git diff --check` PASS; `git diff HEAD -- ml-service/` 0; frozen six byte-identical by clean-tree inheritance; audit `eval/retrieval-v3/audit/events.jsonl` 4 events SHA `90cfb54df614bc59e01551943436fdafc3cd5cac121b071742acbb2fb604c506` byte-identical.
- Canonical v3 result absent; `eval/retrieval-v3/dev/` + `holdout/` absent; no dev-v2 branch/tag/worktree; protected v3 freeze branches untouched.
- No project model override (`opencode.json`/`.opencode.json` absent); root session model `opencode-go/muse-spark-1.3-contributor:xhigh` (runtime context; model config never changed).
- No source-truth/candidate/plaintext read, no `git cat-file`/`show`/`checkout`/`restore`/sparse/worktree, no benchmark/retrieval/ranking/latency/HTTP/model-encode, no ml-service change, no history rewrite in this stage.

## 2. Web blocker (observed, v9 + v9r1 bytes preserved as HOLD evidence)

- Frozen v9r1 `launch_top_level_paseo.py` (14279 bytes, SHA `7c1b31e5be81e5ed364ad117f0eae1f6ab3531edef3827aff9153b78580696b9`) globally scans `~/.omp/agent/sessions/**/*.jsonl` (`SESSION_BASE.rglob("*.jsonl")`) reading the first 10 lines (`MAX_EARLY_LINES=10`) of every session file.
- This violates its metadata-only confinement: it enumerates unrelated sessions and can parse unrelated session message lines while scanning for `type=session`/`type=model_change`. First-10-lines includes title + session + model_change but also reaches message/query/prompt lines in files where early lines are not metadata-only.
- Frozen v9 bytes preserved untouched as HOLD evidence: plan `386bc79a858b9a9fcaf40a7728291c2370b1f97e90a56395266c67f2f03e9e54` (31522), rubric `728fdb9deb06915f17cf0c21f5efca9d29b0ca8b3532b4c884675b9026da63e1` (3330), lock `c27cfef04bb4cf40d4f14d843f05d893778a26df910a0ec37a411812ace47d33` (3947), helper `f91355c7a9b4ac27b7a0847350597b8f21328572a03a339c16303ecc605db1e0` (7588). V9 was never executed.
- Frozen v9r1 bytes preserved untouched as HOLD evidence: plan `2e6f8e0dfa2f25fc0af6f452b97d74658480588f712926eb650db822a207c002` (32875), rubric `e3d1bcf9192b47e71e4d13f815b5aebf957a4ade04672800fb639e8c252c34ec` (3334), lock `47ea88e069ba2da18dbfbb693b4b5ca7020b7425651c38ecc07d7e84d205ac59` (5046). V9r1 was never executed beyond neutral smoke.

## 3. Repair scope (helper ONLY; all semantics identical to v9r1)

- New private builder only: `C:\Users\joji\Documents\programming\bc-v3-dev-v2-builder-20260905-v9r2` (built from v9r1 pre-result inputs/mechanics only; no source truth read at any step).
- New identity only: plan_version `retrieval-v3-dev-generation-v9r2`; seed `benefit-compass-retrieval-v3-dev-v2-generation-v9r2-2026-09-05`; candidate IDs `v3g9r2-001..360`; C opaque IDs `v9r2c-001..360`.
- Identical to v9r1 (verified §6): counts/reserve/location, all 8 authoring semantics incl paired-grounded ambiguous, neutral rubric semantics, A/B-all-360, C-every-360, agreement method, exact lexicographically-smallest selector, candidate-plan gates, detached env sanitization (ALL `PASEO_`/`OMP_` env removed, `PASEO_CLI` captured before sanitization).
- Repaired helper (`launch_top_level_paseo.py`, 16821 bytes, SHA `0d4b9b391272172ec655111ae7ac9b8f9db3cfc7f3651242eea123f256787ca2`, 19-file mechanics set): launch path byte-identical to v9/v9r1 (`get_paseo_cli`/`sanitized_env`/`launch_top_level_agent`/`inspect_agent`/`_parse_ts`/`_norm_cwd` verbatim). `verify_top_level` keeps Paseo checks (ParentAgentId null, cwd exact, model exact equality, provider omp) then proves fallback from the exact OMP session directory only: derive `SESSION_BASE / ('-' + '-'.join(relative.parts))` from expected staging cwd relative to `Path.home()` (staging outside home fails closed with `CONTRACT_INVALID_GENERATION`); poll ONLY that exact directory for `*.jsonl` via non-recursive `exact_dir.glob("*.jsonl")` (never `rglob`, never recurse, never global-scan); per file read AT MOST the first 3 physical lines via three `readline()` calls (ignore line1 title without parsing; require line2 `type=session` with exact cwd + timestamp within 180 s of inspect `CreatedAt`; require line3 `type=model_change` with exact model `opencode-go/muse-spark-1.3-contributor` and `resolvedModelIsFallback` literal `False` via `is not False`; never read line4+ or any message/query/prompt line); require exactly one matching session with bounded polling (5 s interval, 300 s ceiling; zero matches re-poll, multiple fail immediately) else `CONTRACT_INVALID_GENERATION`. Same helper for authors/A/B/C.

## 4. Exclusions (carried byte-identically; no D078/D080 sets)

- 8 fingerprint files carried byte-for-byte from v9r1 `input/` (which carried from v9): failed_d070 `0acc6f27` (273), failed_d071 `3a037d98` (273), failed_d072 `ff3f65d6` (360), failed_d074 `fde76331` (360), failed_d076 `3feaab4d` (365/365), dev-v1 `57716c6a` (180q/228g), holdout `3463a8a1` (250q/212g), history `42e8534d` (248q/248g) — plus `D076_CONTRACT_INVALID_SUMMARY.json` `0279c69a` (aggregate provenance reference). No failed-D078/D080 set (rows 0).
- New v9r2 manifest `input/EXCLUSION_INPUTS.json` SHA `c2180bae5733caa4c99b5326160687511fd4bc86efd5527b3a5cc9ec16701ce3` (2118 bytes, pretty-printed LF like v9r1; same EIGHT gates, required overlap 0; description only).

## 5. Smoke BEFORE freeze (actual, observed, plaintext-free)

- Exactly one neutral disposable child through the repaired helper from THIS agent-scoped root into empty temp `C:/Users/joji/Documents/programming/bc-v9r2-smoke-20260905-neutral` (empty at launch, outside builder, holds no source/candidate content) with pwd-only prompt (no repo/source/project files). Returned `7d5ca7be-1bca-4cc9-9f2c-e9e39c874724`.
- Hard verification PASS via final frozen helper bytes: Paseo `ParentAgentId` null + Cwd exactly staging (`C:\Users\joji\Documents\programming\bc-v9r2-smoke-20260905-neutral`) + Model exactly `opencode-go/muse-spark-1.3-contributor` + Provider `omp`; exact OMP session directory `C:\Users\joji\.omp\agent\sessions\-Documents-programming-bc-v9r2-smoke-20260905-neutral` holds exactly one `*.jsonl` (unique file, matches provenance file); OMP session timestamp `2026-09-05T07:53:41.129Z` vs inspect `CreatedAt` `2026-09-05T07:53:41.777Z`, delta 0.648 s (within 180 s); model exact; `resolvedModelIsFallback` literal false (first-3-lines only, line1 ignored, never line4+).
- Child observed idle; `paseo stop` issued (no-op, already idle). No author/reviewer/C role, no candidate/benchmark/protected action. Staging empty after stop. Pinned in lock.

## 6. Freeze record (frozen BEFORE source-truth content; immutable afterward)

- `GENERATION_PLAN.json` 35727 bytes SHA `2fe9cbe657f12b7aeb3611cad2a367a21692aaec884615625a28781ee1b8407f` (v9r2 identity; supersedes v9r1 plan `2e6f8e0d…`; reserve 42/50/42/50/36/40/46/54=360 + location 12/14/12/16/10/12/14/18=108 unchanged; supersedes reason rewritten for exact-dir + first-3-lines; `v9r1_plan`/`v9r1_rubric`/`v9r1_note` added; manifest SHA `c2180bae…`; rubric SHA `7a1dac2a…`; 19 mechanics SHAs pinned).
- `RUBRIC.json` 3334 bytes SHA `7a1dac2ae4c3c5ef246e2dd2e56e88f7c2d4fec07e2fe1af0824b3b6da7516c3` (`dimensions`/`judgment_rules`/`role` identical to v9r1; diff keys `plan_version` + `rubric_version` only, now `retrieval-v3-dev-generation-v9r2-rubric-v1`).
- `PLAN_LOCK.json` 6395 bytes SHA `a5316374673f771e29413aba242d6fe70b526226e18726b1fdd807723c38ac93` (`frozen_at` `2026-09-05T07:55:35+00:00` truthful observed; source_truth false incl new `v9r1_…_in_this_stage` flags; d070–d080 + v9 + v9r1 semantic flags false; git-object-scan 0; carry-from-v9 + carry-from-v9r1; 19 mechanics SHA-pinned incl repaired helper `0d4b9b39…`; `supersedes_v8_plan` + `supersedes_v9_plan` + `supersedes_v9r1_plan` `2e6f8e0d…`; v9 HOLD SHAs + v9r1 HOLD SHAs preserved; smoke pinned with exact-dir unique-file proof).
- Static verification before source truth (observed): all 19 mechanics compile (LF canonical, no CRLF; `__pycache__` removed); `run_selector.load_plan_binding()` hashes actual plan `2fe9cbe6…` and asserts lock binding PASS (plan/rubric/version/seed); 13 sections (`final_counts`, `reserve_counts`, `reserve_factor`, `authoring_contracts`, `a_b_packets`, `a_b_protocol`, `c_packets`, `c_protocol`, `agreement_diagnostics`, `disagreement_bundle`, `final_selector`, `mechanical_validators`, `standing_contract`) verified identical to v9r1; 8 exclusion sets byte-identical; active-ID residuals 0 (`v3g9r2`/`v9r2c`/`generation-v9r2` only; `v9r1c` appears only as forbidden-old-prefix example in `merge_c.py` comment; `v9`/`v9r1` appear only as HOLD-evidence keys/notes/SHAs in `supersedes`/`dataset_identity`/`post_freeze_rule`); `source_truth.jsonl`/candidates/anchors/sealed/staging absent in v9r2 builder (`input/` holds exactly the 10 fingerprint/manifest files).
- Gate: this record is committed+pushed and remote-verified BEFORE any Phase C source-truth content read. Post-freeze rule in force: NEVER mutate v9r2 plan/rubric/lock; infeasible/contract-invalid → STOP/HOLD, no supplement/relabel/retune/recycle.

## 7. End state and STOP

- Main contains only plaintext-free D-081-SC2 records (this doc + `D-081-SC2` block + SESSION-LOG entry). Private v9r2 builder contains exclusions + scripts + plan/rubric/lock only; `source_truth.jsonl` ABSENT; candidates ABSENT; author/reviewer staging roots NOT created yet (smoke temp dir is neutral, outside builder, holds no source/candidate content; empty after stop).
- STOP for Web independent review. Phase C generation requires Web review/user continuation and is NOT authorized in D-081-SC2. No D-082 execution.

## 8. Forbidden counts (this stage)

Forbidden counts all 0: source-truth snapshot, candidate generation, A/B/C, selector, benchmark/retrieval/ranking/latency/HTTP/model-encode, D068 retry/audit append/result, protected plaintext/recovery, `git cat-file` / `git show` / `checkout` / `restore` / sparse / worktree protected-data/object scanning, global `~/.omp/agent/sessions/**/*.jsonl` scan and first-10-lines/message-content reads (repaired to exact-dir + first-3-lines metadata-only), protected branch/tag/worktree/import, ml-service change, history rewrite, D074/D076/D078/D080/V9/V9R1 row reuse (fingerprint-only exclusion excepted), D-081/v9/v9r1 record mutation, D-082 execution, Desktop/browser/computer use.
