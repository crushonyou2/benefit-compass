# Retrieval v2 — Status (SSOT)

> **Process audit (2026-08-30 durable-only + Web cross-validation addendum) + D-011 Cycle3 clean cycle (2026-08-30, user-confirmed):** [Cycle2 PROCESS AUDIT (v2)](./RETRIEVAL_V2_CYCLE2_PROCESS_AUDIT.md) — 본 SSOT의 `single execution` 표현은 `canonical accepted execution 1회`로 정정. `28/30/30` 등 dev 수치(Phase1 28→30, Exp1~Exp4 30/36 REJECTED)는 기술적으로 유효(`VALID_CANONICAL_RESULT`)하나, **Web session recording으로 Exp1 post-result extra retrieval, Exp2 2회 premature+폐기+steering, Exp3/Exp4 premature+steering이 확정되어 Cycle2 candidate-search 전체는 `PROCESS_CONTAMINATED` — “충분히 탐색했다/더 좋은 후보 없음” 결론은 무효.** 최종 선정은 반드시 fresh holdout에서만 판정. Cycle2 dev 36(SHA `c8b66fef…`)은 D-010상 tuning set 유지 가능하나(역사적 결정 불변), **D-011(2026-08-30, Q-005 closed → D-011, user-confirmed standing decision)에서 보수적 hygiene 경로를 채택하여 Cycle3는 fresh dev 36 + fresh holdout 40을 모두 새로 생성·동결한다.**

> This page is the current SSOT/status page for Retrieval v2. It does not rerun evaluations and does not propose a new candidate.
## Contract

- **D-003** — production retrieval contract (`RERANK=0`, `CANDIDATES=30`, `COSINE_MIN=0.78`, `LEXICAL_OVERLAP_BIAS=0.01`, `strip_region`, expired-policy exclusion, `intfloat/multilingual-e5-base`, source-aware youth bias).
- **D-004** — rejected alternatives remain out of scope (cross-encoder reranking, global similarity/abstention threshold, public region search) unless materially new evidence justifies reconsideration.
- **D-007** — Retrieval v2 evaluation contract. Primary metric source-macro Recall@5; final-holdout quality requires candidate > baseline, net hit@5 ≥ +2, no Youth/Gov24 regression; P0 gates Youth ≥ 28/60 and Gov24 ≥ 15/21; hard-negative paired safety (pure-positive not lower, intrusion not higher); warm paired latency non-regression `candidate p95 <= paired baseline p95`; GO only if all 7 mandatory checks pass (quality improvement, +2 net, no per-source regression, P0 PASS, hard-negative PASS, latency non-regression, holdout integrity). HOLD = fixable mandatory failure; NO-GO = clear quality regression/failure to improve. A GO does not itself authorize production rollout. **Cycle 2 uses same contract; latency requires fresh paired measurement.**
- **D-008** — Retrieval v2 evaluation cycle 1 closes as **HOLD** (2026-08-30). Candidate-v2 and all frozen cycle-1 artifacts remain immutable; no rerun/retune/threshold relaxation to manufacture PASS. Future cycle 2 is a separate evaluation cycle and must not retroactively change cycle-1 HOLD.
- **D-009** — Retrieval v2 evaluation cycle 2 starts (2026-08-30). D-003/D-004/D-007 unchanged; D-008 HOLD immutable; new independent holdout frozen before tuning; cycle-1 results not reused for PASS; latency gate still `candidate p95 <= paired D-003 baseline p95` with fresh measurement; cycle-2 candidate has separate freeze.
- **D-010** — Cycle2 holdout disqualified; Cycle2 bounded to Exp4; Cycle3 new holdout required for final evaluation — 2026-08-30 (Web/user-confirmed, standing decision). (1) Cycle2 holdout `retrieval-v2-cycle2-holdout-v1` (`03da4cc` → `9e2cd6e`, SHA `cf003bab…`) is **disqualified for final evaluation** — post-tuning candidate session에서 HARD SEAL 금지 process-level plaintext read (`_load_holdout_items` fixed ref `git show`)가 실제 발생했으므로 화면/agent-visible 노출·retrieval/rank/score 0이어도 final gate에 사용 금지; `44ce287` repair는 재발 방지이며 과거 위반을 소급 무효화하지 않음. (2) Cycle2 frozen dev `retrieval-v2-cycle2-dev-v1` (`500bead` → `372ed68`, SHA `c8b66fef…`, 36 cases)는 정상 tuning set으로 유지. (3) Cycle2 candidate search는 **Exp4를 마지막 bounded experiment로 제한** — Exp4 REJECTED면 Cycle2 종료, Exp4 통과 시 해당 candidate 고정 후 별도 Cycle3에서 완전히 새로운 holdout을 tuning 전 생성/봉인한 뒤 그 candidate에 **추가 tuning 없이** 평가. (4) D-003/D-004/D-007/D-008/D-009 유지, Cycle1 HOLD 불변, gate/threshold 완화 없음, 기존 Cycle2 holdout artifacts/tag는 immutable history로 보존하되 final gate evidence로 사용 금지.
- **D-011** — Adopt Retrieval v2 Cycle3 clean evaluation/candidate-search — fresh dev+holdout, pre-registration, isolation, audit log — close Q-005 — 2026-08-30 (user-confirmed standing decision, Q-005 closed → D-011). (1) Cycle3를 clean cycle로 시작. (2) Cycle2 canonical metric/artifact(`VALID_CANONICAL_RESULT`, 28/30/30, dev `c8b66fef…`, holdout `cf003bab…`)는 immutable historical diagnostic evidence로 보존하되 `PROCESS_CONTAMINATED`이므로 Cycle3 selection/final evaluation 근거로 재사용 금지. (3) D-010 'dev 36 tuning set 유지 가능'은 역사적 결정으로 불변이나 Cycle3에서는 보수적 hygiene로 Cycle2 dev 36 재사용 없이 **fresh dev 36 + fresh holdout 40을 모두 새로 생성·동결**. (4) fresh dev/holdout 모두 candidate tuning 전 독립 생성·동결, P0/cycle1 dev+holdout/cycle2 dev+disqualified holdout/hard-negative 및 서로 간 query+gold overlap 0 fail-closed 검증; holdout/dev builder 세션은 candidate-tuning 세션과 분리. (5) candidate 개발 전 후보 설계 공간·최대 실험 수 pre-register; 이후 Web은 Paseo 체크리스트·최종 보고 완료 후에만 독립 교차검증(HARD GATE/보안·seal 위반만 즉시 중단 예외). (6) Cycle3부터 retrieval/protected-set 접근에 append-only run/access audit log 도입; final holdout plaintext는 candidate freeze + independent review + 사용자 명시 승인 전 접근 금지. (7) dev에서 사전등록 selection 조건 통과 후보만 freeze 후 fresh holdout에서 추가 tuning 없이 D-007 7 mandatory gates 평가. (8) D-003/D-004/D-007/D-008/D-009/D-010 및 Cycle1 HOLD 유지, gate/threshold 완화 없음, production rollout 별도 결정.
## Cycle 1 — frozen candidate

| item | value |
|---|---|
| candidate tag | `retrieval-v2-candidate-v2` |
| candidate commit | `5745cc3144b519da456b21030d0e0752d1d018ae` |
| artifact commit | `c6c082681b4f2fcd521790e50c5fd46549116307` |
| manifest | `eval/retrieval-v2/candidate/manifest.json` LF SHA256 `86f80ff6389ede4673e3c8d819cfab2ceefc79b8979a68b7b2bb5d64cc8eccff` |
| config | `lexical-rewrite-v1` (`lexical_overlap_terms_rewrite` — particle-stripped stem replacement, `MIN_STEM_LEN 2`) |
| production parity | `strip_region`, youth intent bias suppressed for Gov24 orgs, `LEXICAL_BIAS 0.01`, `CANDIDATES 30`, `COSINE_MIN 0.78`, no cross-encoder/threshold/region search |

## Cycle 1 — gate outcomes (no rerun; artifact/tag cross-verified)

| gate | result | detail | artifact | tag → commit |
|---|---|---|---|---|
| Final holdout quality (D-007) | **PASS** | 40 queries (Youth 20 + Gov24 20). Baseline 33/40 → candidate 36/40, source-macro 0.825 → 0.900, net +3 (gains holdout-001/028/036, losses 0), Youth 18/20→20/20, Gov24 15/20→16/20 | `eval/retrieval-v2/final/summary-v1.json` | `retrieval-v2-final-holdout-result-v1` → `d86e0119f9ac5cf3028364df24d898ff638d3b76` |
| P0 regression | **PASS** | Youth 28/60, Gov24 16/21 | `eval/retrieval-v2/p0/p0-candidate-v2.json` | `retrieval-v2-p0-result-v1` → `3373da294b73705861b7a0e494ba802f9e9f6786` |
| Hard-negative paired safety | **PASS** | pure-positive 15/21→16/21, excluded-policy intrusion 0/3→0/3 | `eval/retrieval-v2/hard-negative/paired-candidate-v2.json` | `retrieval-v2-hard-negative-result-v1` → `34ca5a537f0a537b9217e3b2fffd005b80a5fe19` |
| Warm paired latency (D-007) | **HOLD** | baseline p95 476.51 ms, candidate p95 480.55 ms, Δ +4.04 ms; D-007 requires `candidate p95 <= baseline p95`. p50 baseline 410.40 → candidate 395.82 (diagnostic), n=180 per variant (360 total), warmup 36/variant, interleaved paired design | `eval/retrieval-v2/latency/latency-candidate-v2.json` | `retrieval-v2-latency-result-v1` → `b04556f9251d6cabadd32c7c39c85dee690c8b48` |

Additional immutable refs: holdout `retrieval-v2-holdout-v1` (`12515a20758265b0b5a5f52acef5aa40de3b6253`, SHA `02eb03866f8e09b66ea7c3b83856fe939ee0b966350053277aaca3f2d7121eda`, 40 cases), evaluator `retrieval-v2-holdout-evaluator-v1` (`e32d3ebee871918bccf08613e34ae7a72d953737`), p0 evaluator `retrieval-v2-p0-evaluator-v1` (`63b9ac62dc980ca0a1ab84fe90456e85cdae1a18`), hard-negative evaluator `retrieval-v2-hard-negative-evaluator-v1` (`ba2b3099ea0daf67f47453390c24ccbc9a389819`), latency evaluator `retrieval-v2-latency-evaluator-v2` (`7b8c4ea868afc3eb8b4ab33f63b067bd23c087ba`, harness LF `66a4e48e9c71ecd03aa389ac93ac651817d3147355cb40d64511044357ac26e0`).

## Provenance & review

- Latency provenance SSOT: `retrieval-v2-latency-provenance-v3` — tag object `c0d2a9321114144b5ab4235a66c80faf6f112c57` → commit `3ac62181de9c343511adfb2db82cb0cc64b36009` on branch `codex/retrieval-v2-latency-provenance-recovery`.
- Independent reviewer verdict: **APPROVE** — measurement **provenance blocker resolved**, not latency PASS. Latency numerical gate remains HOLD.
- Superseded tags `retrieval-v2-latency-provenance-v1/v2` remain immutable audit history.
- Recovery scope: 3 files only (`PROVENANCE-RECOVERY.md`, `provenance-attestation-v1.json`, `test_retrieval_v2_latency_provenance_attestation.py`); no measurement rerun.

## Overall verdict

- **Cycle 1 = HOLD** per D-007/D-008. Quality / P0 / hard-negative success is explicitly distinguished from mandatory **latency failure**.
- Evaluation **GO not granted**; **production rollout not authorized**.
- Cycle-1 HOLD is durable and not retroactively changeable by a future cycle.

## Cycle 2 — CLOSED — Phase1/correction/Exp1/Exp2/Exp3/Exp4 REJECTED; holdout DISQUALIFIED (D-010); Cycle2 candidate search CLOSED (D-010)

- **Status (D-010 in force, Cycle2 CLOSED):** D-009 started 2026-08-30; holdout frozen before tuning; dev frozen before tuning; Phase1 diagnostic completed (diagnostic_only), correction completed, Exp1 REJECTED, Exp2 REJECTED (single execution at 53bd190 dirty, fail-closed), Exp3 REJECTED (single execution at 44ce287 dirty, fail-closed), **Exp4 REJECTED (single execution at beb9828 dirty, fail-closed, 2026-08-30) — Cycle2 candidate search CLOSED per D-010** — no additional Exp, no candidate fixation. **Cycle2 holdout `retrieval-v2-cycle2-holdout-v1` (`03da4cc` → `9e2cd6e`, SHA `cf003bab…`) is DISQUALIFIED for final evaluation per D-010** — post-tuning candidate session process-level plaintext read 발생으로 final gate 사용 금지 (화면/agent-visible·retrieval 0이어도 불변). `44ce287` repair는 재발 방지이며 과거 위반 소급 무효화 아님. **Frozen dev `retrieval-v2-cycle2-dev-v1` (`500bead` → `372ed68`, SHA `c8b66fef…`, 36 cases)는 정상 tuning set으로 유지하되 Cycle2에서는 추가 candidate 탐색 없음; Cycle3 new holdout 필요.** No final gate, no GO. D-010 bounded Exp4 is last experiment — CLOSED.
- **cycle2-start:** branch `codex/retrieval-v2-cycle2-start` from cycle-1 HOLD `5311e9807bab43f869655e13d4cdd006123f1ed5` → commit `434b798d60bf15433590362aaad4a021846094d4`; tag `retrieval-v2-cycle2-start-v1` object `f8d03d1b5565cd1e6dcda341bfe92230c7dab4cd` peeled `434b798` (verified remote).
- **holdout freeze (DISQUALIFIED, immutable history only):** remote branch `codex/retrieval-v2-cycle2-holdout-freeze` → commit `9e2cd6ea4b8203b474d7d6a6a69a088763284043`; tag `retrieval-v2-cycle2-holdout-v1` object `03da4cc28d1bb324f5176efb500dfeaa1684b3fa` peeled `9e2cd6ea4b8203b474d7d6a6a69a088763284043` (verified remote). **D-010에 따라 final gate evidence로 사용 금지 — immutable history로만 보존.**
- **holdout aggregates (plaintext-free, DISQUALIFIED):** n=40 Youth 20 / Gov24 20, categories housing_finance 7 / family_care 7 / employment_education 7 / welfare_health 7 / culture_community 6 / business_agriculture 6, evalset `eval/retrieval-v2/cycle2/holdout/evalset.jsonl` LF SHA256 `cf003bab7713138fbd9c4622addeeb886c01f401aeab3d43b1144ae6e4c79727`, P0/dev/cycle1-holdout/hard-negative query+gold overlaps all 0, `retrieval_observed=false`, `candidate_tuning_started=false`, sealed before tuning — **seal violation으로 final evaluation 사용 불가 (D-010).**
- **dev freeze (RETAINED as tuning set, D-010):** branch `codex/retrieval-v2-cycle2-candidate` → commit `372ed686579b4e8e2b9854d297e44fee18775352`; tag `retrieval-v2-cycle2-dev-v1` object `500beadae11ddb423cc2ea4d46494c0a9f2b1173` peeled `372ed686579b4e8e2b9854d297e44fee18775352` (verified remote).
- **dev aggregates (plaintext-free, retained):** n=36 Youth 18 / Gov24 18, categories housing_finance 6 / family_care 6 / employment_education 6 / welfare_health 6 / culture_community 6 / business_agriculture 6 (each 3/3 balanced), evalset `eval/retrieval-v2/cycle2/dev/evalset.jsonl` LF SHA256 `c8b66fef69bdfd0db053ac7cac0fb027fc3271c6072ab992b622cacdc71ace5e`, P0/cycle1-dev/cycle1-holdout/cycle2-holdout/hard-negative query+gold overlaps all 0, `retrieval_observed=false`, `candidate_tuning_started=false`, frozen before tuning — **D-010에 따라 정상 tuning set으로 유지, 후보 개발에 계속 사용 가능.**
- **candidate branch current:** branch `codex/retrieval-v2-cycle2-candidate` — Exp2 execution baseline `53bd190cd716cce8a81a1ff3979483098f78471d` (pre-Exp2, Phase1/correction/Exp1 REJECTED state, docs/RETRIEVAL_V2 at 53bd190) and Exp2 result artifacts committed in `b1faabec2a0210070f565a918c18d3c4d762f37e`; Exp3 execution at `44ce287`; Exp4 execution at `beb9828a69432477c0cb22b8d776fc800a90dbfe` dirty (REJECTED, Exp4 last bounded, Cycle2 CLOSED); current HEAD `85b92efa52264c1878279746cf825043cca9cc4d` (post-Exp4 CLOSED, D-010). Dev plaintext `eval/retrieval-v2/cycle2/dev/` present, holdout absent. Phase1 `phase1-*`, Exp1 `phase2-exp1-region-hint/`, Exp2 `phase2-exp2-embedding-region/`, Exp3 `phase2-exp3-semantic-core/`, Exp4 `phase2-exp4-region-attached/` all present — Cycle2 CLOSED.
- **Phase1 diagnostic (diagnostic_only, not_final_gate):** commit `6d743bb366530cb34d03a3efd0a7860e221421c5` — baseline R@5 28/36 (Youth10/18 Gov24 18/18) vs candidate-v2 R@5 30/36 (Youth12/18 Gov24 18/18) net +2 gains `c2d-025` `c2d-031` losses 0; MRR baseline 0.6577 vs candidate 0.6884; latency diagnostic p95 baseline 487.31 ms vs candidate 546.50 ms delta +59.18 ms (180/variant interleaved, warmup 18)
- **Correction:** commit `c2dfd87bf6602e78bef5ecbc09d297bfbf2a6f74` — corrected Phase1 `filtered_by_cosine` to threshold-only and added `outside_top10_after_threshold` for `c2d-025`. No quality/latency numbers changed.
- **Exp1 — bounded region-core lexical hint (lexical-only, 1 SIDO/canonical per matched code):** commit `22d6e6c32b9a443d963d2db67698e779ec07a42d` — **REJECTED** (new 30/36 not >30, R@1 22/36 vs 21/36, MRR 0.7069 vs 0.6884), baseline 28 vs candidate 30, Gov24 18/18, loss0 vs candidate, hinted 23/36 avg1.0, latency NOT_RUN_EARLY_STOP, artifact `phase2-exp1-region-hint/phase2-exp1-paired.json`, youth_intent_bias 0.01→0.015 patched at 53bd190.
- **Exp2 — embedding preserves at most one SIDO via earliest alias (lexical unchanged):** **REJECTED** — single execution on frozen dev 36 in `53bd190` dirty working tree, result committed in `b1faabec2a0210070f565a918c18d3c4d762f37e` — fail-closed asserts baseline 28/36 / candidate 30/36 / Gov24 18/18 passed, per-case `new.selected_sido_code` via `_earliest_sido_code` recorded, vector_only extra DB 2×/case removed (rank_top30 candidate vs new compared, lexical identical). Quality: baseline R@1 21/36 R@5 28/36 R@10 29/36 MRR 0.6577 macro 0.7778; candidate-v2 R@1 21/36 R@5 30/36 R@10 30/36 MRR 0.6884 macro 0.8333; new R@1 25/36 R@5 30/36 R@10 30/36 MRR 0.7509 macro 0.8333; Youth/Gov24 new 12/18 /18/18; baseline vs new net +2 gains2 `c2d-025` `c2d-031` loss0, candidate vs new net 0 gains0 loss0. `new 30 not >=31` → **REJECTED**, latency **NOT_RUN_EARLY_STOP**.
- **Incident — holdout plaintext audit seal repair (aggregate-only, Exp3 pre-retrieval, no retrieval/DB/model):** post-tuning candidate session에서 `eval/test_retrieval_v2_cycle2_devset.py::_load_holdout_items()`가 고정 ref `git show`로 holdout plaintext를 process 메모리에 읽음 — `stdout`/agent-visible case content·retrieval·rank·score 노출 0, Exp3 retrieval 미실행. HARD SEAL 충돌로 판단하고 `RETRIEVAL_V2_ALLOW_HOLDOUT_PLAINTEXT_AUDIT=1` opt-in 없이는 `_load_holdout_items()`가 `git show` 호출 전 `unittest.SkipTest`로 fail-safe 차단되도록 repair (`44ce287`). default candidate-session은 holdout ref를 절대 읽지 않음; opt-in에서만 기존 fail-closed audit(40건 집계) 유지. 대상: cycle1/cycle2 holdout query/gold overlap 4건 + helper 1건이 default에서 skipped, dev 무결성 나머지는 pass. 본 incident는 seal 완화가 아니며, **D-010에서 해당 holdout은 최종 평가 사용 불가로 확정**.
- **Exp3 — semantic-core embedding (`" ".join(lexical_overlap_terms_rewrite(strip_region(raw)))` or fallback, lexical identical):** **REJECTED** — single execution on frozen dev 36 at `44ce287 dirty` (HARD SEAL repair 후 clean baseline), fail-closed dev SHA `c8b66fef…` 및 `baseline 28/36 Youth10 Gov2418 / candidate-v2 30/36 Youth12 Gov2418` sanity 통과. Quality: baseline R@1 21/36 R@5 28/36 R@10 29/36 MRR 0.6577 macro 0.7778 Youth10/Gov24 18; candidate-v2 R@1 21/36 R@5 30/36 R@10 30/36 MRR 0.6884 macro 0.8333 Youth12/Gov24 18; new R@1 23/36 R@5 30/36 R@10 30/36 MRR 0.7116 macro 0.8333 Youth12/Gov24 18; baseline vs new net +2 gains `c2d-025` `c2d-031` loss0, candidate vs new net 0 gains0 loss0. `new 30 not >=31`로 3조건(new>=31 && Gov24==18 && loss0) 중 1개 실패 → **REJECTED**, latency **NOT_RUN_EARLY_STOP**.

- **Exp4 — region-attached residue cleanup embedding (`cleanup_embedding_query(raw)` alias+suffix(max1 longest)+particle(max1 longest) directly attached, fallback `strip_region(raw)`, lexical identical to candidate-v2):** **REJECTED** — single execution on frozen dev 36 at `beb9828 dirty True` (2026-08-30, HOLDOUT opt-in off, `RETRIEVAL_V2_ALLOW_HOLDOUT_PLAINTEXT_AUDIT` unset, holdout plaintext 0, `git show`/`checkout` 0, tag/freeze 금지 준수), `intfloat/multilingual-e5-base`, same process/DB/corpus/SQL (`total_policies 13589 total_chunks 17609 gov24 10958 youth 2631`), `git beb9828 dirty True`, corpus provenance singular. Quality: baseline R@1 21/36 R@5 28/36 R@10 29/36 MRR 0.6577 macro 0.7778 Youth10/Gov24 18; candidate-v2 R@1 21/36 R@5 30/36 R@10 30/36 MRR 0.6884 macro 0.8333 Youth12/Gov24 18; new R@1 21/36 R@5 30/36 R@10 30/36 MRR 0.6884 macro 0.8333 Youth12/Gov24 18; baseline vs new net +2 gains `c2d-025` `c2d-031` loss0, candidate vs new net 0 gains0 loss0, Gov24 18/18, loss0; `new 30 not >=31`로 3조건(new>=31 && Gov24==18 && loss0) 중 1개 실패 → **REJECTED**. `embedding_changed 8/36` (q_new != q_stripped), `cleanup_applied 23/36`, production diff 0, lexical identical. Latency **NOT_RUN_EARLY_STOP** — quality REJECTED이므로 latency 180/variant diagnostic_only/not_final_gate 미실행. Artifact `phase2-exp4-region-attached/phase2-exp4-paired.json` + `phase2-exp4-summary.md` (R@1/R@5/R@10/MRR/macro, Youth/Gov24, gains/losses, REJECTED, NOT_RUN_EARLY_STOP). D-010 bounded last Exp — **Cycle2 CLOSED**.

## Next state (D-011) — Cycle3 BOOTSTRAPPED (2026-08-30, `codex/retrieval-v2-cycle3-start` from `5cabd2e` — pre-registered, no retrieval yet)

- **Cycle3 is BOOTSTRAPPED (D-011, 2026-08-30, Q-005 closed → D-011, user-confirmed).** Cycle2 canonical metric/artifact(Phase1 28→30, Exp1~Exp4 30/36 REJECTED, `VALID_CANONICAL_RESULT`, dev SHA `c8b66fef…`, holdout SHA `cf003bab…`, corpus `13589/17609`, production diff 0)는 immutable historical diagnostic evidence로 보존하되, Web session recording으로 Exp1 post-result extra retrieval, Exp2 2회 premature+폐기+steering, Exp3/Exp4 premature+steering이 확정되어 Cycle2 candidate-search 전체가 `PROCESS_CONTAMINATED`였으므로 **Cycle3의 candidate selection/final evaluation 근거로 재사용하지 않는다**. "충분히 탐색했다/더 좋은 후보 없음" 결론은 무효. D-010의 'Cycle2 dev 36(SHA `c8b66fef…`) tuning set 유지 가능'은 역사적 결정으로 불변이나, Cycle3에서는 더 보수적인 hygiene 경로를 채택하여 **Cycle2 dev 36을 재사용하지 않고 fresh dev 36 + fresh holdout 40을 모두 새로 생성·동결**한다. PLANNED → BOOTSTRAPPED.
- **Hygiene & isolation (D-011 §4):** fresh dev(36, Youth 18/Gov24 18)와 fresh holdout(40, Youth 20/Gov24 20)은 candidate tuning 전에 서로 다른 sanitized builder context에서 생성·검증·동결 완료. Historical 6-set fingerprint union 및 fresh dev↔holdout query+gold overlap은 모두 0으로 fail-closed 검증했다. Holdout plaintext는 protected branch/tag `codex/retrieval-v2-cycle3-holdout-freeze` / `retrieval-v2-cycle3-holdout-v1`에만 존재하며 candidate/dev branch에는 포함하지 않는다. Dev는 `codex/retrieval-v2-cycle3-dev-freeze`에서 동결하고 이후 candidate tuning 입력으로만 사용한다.
- **Pre-registration & Web isolation (D-011 §5):** candidate 개발 전에 후보 설계 공간과 최대 실험 수를 pre-register 완료 — **pre-reg max=3** (`c3e1-vector-pool-128` / `c3e2-vector-pool-256` / `c3e3-vector-pool-512`), dev 결과를 본 뒤 추가·변경·재실행 금지, 단일 canonical dev batch에서 baseline과 함께 3 후보 모두 평가. SQL은 `nearest → vector_pool K → lexical on K only → youth/lexical ordering → final n=30` bounded pre-pool, D-004 준수. 세부: `docs/RETRIEVAL_V2_CYCLE3_PREREG.md` + `eval/retrieval-v2/cycle3/prereg-v1.json` (machine-readable). 이후 Web은 Paseo 체크리스트와 최종 보고 완료 후에만 독립 교차검증(HARD GATE/보안·seal 위반만 즉시 중단 예외).
- **Audit log & seal (D-011 §6):** Cycle3부터 retrieval 실행 및 protected-set 접근에 대해 append-only run/access audit log 도입 — `eval/retrieval_v2/cycle3_audit.py` + `eval/retrieval-v2/cycle3/audit/events.jsonl` (JSONL, schema_version/event_id/utc_timestamp/git_head/git_dirty/process_id/session_id/action/candidate_id/set_role/set_sha/command/runner_id/outcome/previous_event_hash/event_hash, chain fail-closed, holdout access gate). final holdout plaintext는 candidate freeze + independent review + 사용자 명시 승인 전 접근 금지. bootstrap에서는 infrastructure test용 temp file만 사용, 실제 retrieval/access event 미기록.
- **Fingerprint (D-011 §5):** protected-set overlap을 holdout plaintext 재열람 없이 검증하기 위한 generic collision fingerprint helper — `eval/retrieval_v2/cycle3_fingerprint.py` (`SHA256(normalized query)` + `SHA256(source+NUL+source_id)`, manifest 저장, overlap 0 pure helper). bootstrap에서는 실제 fresh dev/holdout 생성 없음, 기존 cycle1/2 protected plaintext를 읽어 fingerprint 새로 만들지 않음.
- **Gating (D-011 §7):** dev에서 사전등록 selection 조건을 통과한 후보만 freeze하고 independent review 후 fresh holdout에서 추가 tuning 없이 D-007의 7 mandatory gates(quality improvement, +2 net, no Youth/Gov24 regression, P0 PASS, hard-negative PASS, latency non-regression, holdout integrity)를 평가한다. 하나도 통과 못 하면 holdout 평가 없이 Cycle3 종료.
- **Contracts unchanged (D-011 §8):** D-003/D-004/D-007/D-008/D-009/D-010 및 Cycle1 HOLD는 역사/계약으로 유지한다. gate/threshold 완화 없음. production rollout은 별도 결정이다. Cycle2 holdout artifacts/tag(`retrieval-v2-cycle2-holdout-v1`, `codex/retrieval-v2-cycle2-holdout-freeze`, `9e2cd6e` commit, SHA `cf003bab…`)와 Cycle2 dev(`retrieval-v2-cycle2-dev-v1`, `c8b66fef…`, 36 cases)는 immutable history로 보존하되 Cycle3 gate evidence로 재사용하지 않는다(단, dev는 D-010상 tuning set 유지 가능하나 Cycle3에서는 fresh로 교체).
- **Cycle2 closure preserved:** Cycle2 candidate search는 D-010에 따라 Exp4 REJECTED(2026-08-30 `beb9828` dirty, 30/36 vs candidate 30 vs baseline 28, Gov24 18/18, loss0, `new 30 not >=31` → REJECTED, latency NOT_RUN_EARLY_STOP, embedding_changed 8/36, cleanup_applied 23/36)로 **CLOSED** — 추가 Exp 없음, candidate fixation 없음. Phase1, correction, Exp1, Exp2, Exp3, Exp4 all **REJECTED**.
- **Bootstrap provenance (Cycle3 BOOTSTRAPPED, no retrieval):** base `5cabd2eecd78923da4751c5e60fa316e74f563fc` on `codex/retrieval-v2-cycle2-candidate` → branch `codex/retrieval-v2-cycle3-start`, tag `retrieval-v2-cycle3-start-v1` (annotated, immutable) → this commit. pre-registration `docs/RETRIEVAL_V2_CYCLE3_PREREG.md` + `eval/retrieval-v2/cycle3/prereg-v1.json` fixed, audit `eval/retrieval_v2/cycle3_audit.py` + tests, fingerprint `eval/retrieval_v2/cycle3_fingerprint.py` + tests, `eval/retrieval-v2/cycle3/audit/README.md`. **No retrieval/DB/model/embedding/benchmark/holdout plaintext 생성·접근·`git show`/`checkout`, no production/ml-service 수정, no existing eval result artifact 수정, no fresh dev/holdout plaintext 생성·접근, no existing cycle1/2 protected plaintext 열람 in this bootstrap — infrastructure/audit + spec only. Web은 Paseo 작업 완료 전 개입하지 않음.**

## Cycle 3 — BOOTSTRAPPED — pre-registration / audit infra (no retrieval yet)

- **Status:** DATASETS FROZEN — spec + historical fingerprint catalog + fresh holdout 40 + fresh dev 36 동결 완료, **no retrieval yet**. D-011 clean cycle, pre-reg max=3, candidate implementation/execution not started.
- **Pre-registration:** 3 candidates fixed (`c3e1-vector-pool-128` K=128, `c3e2-vector-pool-256` K=256, `c3e3-vector-pool-512` K=512), SQL bounded pre-pool semantics normative (see `docs/RETRIEVAL_V2_CYCLE3_PREREG.md` §5), D-003/D-004 unchanged, `lexical_overlap_terms_rewrite(strip_region(raw))` preserved.
- **Fresh holdout 40:** protected freeze branch `codex/retrieval-v2-cycle3-holdout-freeze`, annotated tag `retrieval-v2-cycle3-holdout-v1`, 40 cases (Youth20/Gov2420; categories 7/7/7/7/6/6), canonical LF evalset SHA256 `4c631ce7cdcc03374bb1861d0a27e0ebbacf35a691fb6f54543b96c7f051c350`, historical overlap q/g `0/0`, internal duplicate `0/0`, `well_posed 40 / ambiguous 0`. Holdout plaintext는 candidate freeze + independent review + 사용자 명시 승인 전 접근 금지.
- **Fresh dev 36:** `codex/retrieval-v2-cycle3-dev-freeze`, 36 cases (Youth18/Gov2418; 6 categories each 6, 각 category source 3/3), canonical LF evalset SHA256 `3791368f4722b612058b7a005e17bf5f1caae4ac0437daa9d44ff28f28ca260c`, historical overlap q/g `0/0`, frozen holdout fingerprints 대비 q/g `0/0`, internal duplicate `0/0`, `well_posed 36 / ambiguous 0`. Selection rule는 prereg 그대로: quality-selectable (R@5>baseline, net≥+2, Youth0, Gov24 0) then warm paired latency `candidate p95 <= baseline p95`, tie-break net→macro→delta→smaller K. Zero DEV_SELECTABLE → holdout 평가 없이 종료.
- **Audit / fingerprint:** append-only JSONL chain (previous_event_hash/event_hash, fail-closed, holdout gate) + deterministic SHA256 fingerprints (query normalized, gold source+NUL+id) — infrastructure only, temp-file tests, no real events/plaintext in bootstrap.

## Cycle 3 — INFRA REPAIR v2 (2026-08-30, `codex/retrieval-v2-cycle3-start` — fail-closed repairs, no retrieval yet)

- **Scope:** INFRA REPAIR only — Web 독립검증에서 재현된 2건 fail-open (`check_overlap({}, {}, strict=True)` PASS, stale `set_sha` grant PASS) 및 추가 fail-closed 결함을 보수. No retrieval/DB/model/embedding/benchmark, no fresh dev/holdout, no cycle1/2 plaintext, no production/ml-service, no result artifact 수정.
- **Bootstrap tag preserved:** `retrieval-v2-cycle3-start-v1` (`2a30e8d371...` → `e4e56198...`) 불변 유지; `prereg-v1.json` 후보/selection 불변, `created_at` nominal vs canonical `2026-08-30T18:34:01+09:00` (`09:34:01Z`)는 provenance addendum에서 명시 (`docs/RETRIEVAL_V2_CYCLE3_PREREG_ADDENDUM.md`, `eval/retrieval-v2/cycle3/prereg-v1.provenance.json`, SHA256 `18B6C997...` 재검증).
- **Infra-repair tag:** `retrieval-v2-cycle3-infra-v2` (annotated, this repair) — single commit on `codex/retrieval-v2-cycle3-start` after bootstrap.
- **Audit repairs:** git provenance probe failure → FAIL (no `unknown`/`False` fallback), UUID v4 strict, UTC Z timestamp strict, `git_dirty` bool / `process_id` positive int / `session_id` non-empty strict, `set_sha` semantics (`dev`/`holdout`→`64-hex`, `none`→`null`), `protected_access` action/role fail-closed, holdout gate exact `set_sha`+`session_id`+ latest start not closed + outcome `success`/`allowed` only + optional `expected_event_hash` token to block stale grants. See `docs/RETRIEVAL_V2_CYCLE3_INFRA_REPAIR.md` §2.
- **Fingerprint repairs:** `validate_fingerprint_manifest()` strict (`v1`+spec, keys/list, 64-hex, duplicate error, `cases` count exact), `manifest_with_fingerprints()` duplicates/count hide 없이 FAIL, `check_overlap()` validator 통과 후에만 overlap 계산. See `docs/RETRIEVAL_V2_CYCLE3_INFRA_REPAIR.md` §3.
- **Tests:** regression `eval/test_retrieval_v2_cycle3_infra_repair.py`가 `missing keys/wrong version/invalid hash/duplicate/count mismatch/stale sha/session/end/outcome/UUID/timestamp/git provenance` 전부를 직접 증명. 기존 `test_retrieval_v2_cycle3_*`도 strict schema에 맞춰 갱신·PASS. pure/static only, `py_compile`/`git diff --check`/`ml-service` diff 0 등 gate PASS 후 infra-repair commit/tag push.
## Cycle 3 — CANONICAL RUNNER IMPLEMENTATION (2026-08-31, `codex/retrieval-v2-cycle3-candidate` — implementation only, no execution)

- **Scope:** canonical runner 구현 + pure/static/mock tests + self-review + single atomic commit + push — **canonical dev batch 실제 실행 금지**, 실제 retrieval/DB/query/model/embedding/benchmark/latency 실행 금지, production `ml-service` behavior 변경 금지, prereg 후보/K/selection rule/threshold/SQL semantics 변경 금지, 새 후보 추가 금지, result artifact 생성 금지. 이 sparse isolated Git worktree에서는 `eval/retrieval-v2/cycle3/dev/evalset.jsonl` 및 `annotation_audit.json`이 sparse로 ABSENT이며 `git show`/`cat-file`/checkout/restore/sparse-checkout 변경/parent worktree 접근으로 복구·열람하지 않음. `HEAD cb47935c8d9c4ea6d1e45a91e207bc6aa1223c5b` clean.
- **Runner 구현 계약:**
  - baseline + 정확히 `c3e1-vector-pool-128` (K=128) / `c3e2-vector-pool-256` (K=256) / `c3e3-vector-pool-512` (K=512), `FINAL_N=30` 고정 — `eval/retrieval_v2/cycle3_runner.py:get_candidate_registry()` fail-closed.
  - prereg exact normative SQL/ordering 그대로: `WITH nearest → vector_pool LIMIT K → lexical on K only → youth/lexical ordering → LIMIT 30`, deterministic `t.dist, t.source, t.source_id` tie-break, `rp IS NULL` only, lexical은 K에만 적용 — `get_sql_for_candidate()`/`validate_sql_semantics()`로 고정.
  - candidate-v2 lexical semantics / D-003 / D-004 / D-007 유지, `RERANK=0`, `COSINE_MIN=0.78`, `LEXICAL_BIAS=0.01`, `strip_region`/`lexical_overlap_terms_rewrite` delegations 불변, `apply_cosine_filter()`는 SQL 바깥 post-LIMIT.
  - single canonical dev batch에서 baseline+3을 한 번에 실행하는 구조, 배치 identity `cycle3-canonical-dev-v1` 및 `validate_single_batch_request()` guard로 결과 후 개별 재실행/새 K/새 후보 차단.
  - quality/selection rule prereg 그대로 (`macro R@5 > baseline` + `net hit@5 >=+2` + `Youth/Gov24 regression 0`), quality-selectable만 predefined paired latency로 이동하도록 `dev_selectable()` 경계 — 이번 stage에서 실제 latency 실행 없음.
  - Cycle3 audit fail-closed integration: `require_protected_dev_access_grant()`는 실제 dev plaintext open 직전 exact `set_sha`/`session_id`/`expected_event_hash` 검증, stale/failure/no-grant/closed/token-mismatch 모두 fail-closed. 실제 audit log에는 이번 stage에서 `run_start`/`protected_access` event를 쓰지 않음 (tests는 temp audit + synthetic/mock만).
  - holdout evaluation/access path는 `assert_holdout_blocked()`/`assert_not_holdout_path()`로 candidate freeze + independent review + 사용자 명시 승인 전까지 차단.
  - deterministic ordering `ordering_key()`, candidate registry, result schema/provenance `build_result_skeleton()`/`validate_result_schema()` 단일-batch guard를 `eval/test_retrieval_v2_cycle3_runner.py` (50 tests, pure/static)로 고정.
- **Files:** `eval/retrieval_v2/cycle3_runner.py` (normative runner library), `eval/retrieval_v2/run_cycle3_canonical_dev.py` (single-batch CLI, fail-closed guards, NOT executed in this stage), `eval/test_retrieval_v2_cycle3_runner.py` (pure/static runner contract tests).
- **Validations (pure/static only, dev plaintext 미요구):** `python -m py_compile` PASS, `git diff --check` PASS, `ml-service` diff 0, `pytest eval/test_retrieval_v2_cycle3_runner.py` 50 passed, `pytest eval/test_retrieval_v2_cycle3_prereg.py ..._infra_repair.py ..._audit.py ..._fingerprint.py ..._historical_catalog.py ..._scaffold.py` 183 passed (excluded-plaintext 불필요 테스트만), sparse dev plaintext 미접근 검증.
- **Next:** Web이 commit 후 full repo에서 전체 Cycle3 suite를 독립 실행하여 검증한 뒤, 별도 canonical execution 세션에서 단일 canonical dev batch를 audit grant 하에 1회 실행 (holdout 평가는 freeze+review+approval 전 금지).
## Cycle 3 — CANONICAL RUNNER HARDENING — Web HOLD repair (2026-08-31, `ce0ecac` → new HEAD, sparse isolated, no execution)

- **Trigger:** Web independent review of `ce0ecac` issued **HOLD** on three blockers: **A)** `run_cycle3_canonical_dev.py` calls `assert_rp_is_null(None)` without `assert_rp_is_null` import → `NameError` before retrieval; **B)** Actual canonical execution orchestration not implemented — code after `CYCLE3_CANONICAL_EXECUTION` gate was comments/placeholders ending in unconditional `_fail_closed()` (not execution-ready); **C)** Hard path confinement `posixpath.normpath(p.replace('\\\\','/').lstrip('./'))` allows `../../../eval/...` to normalize to canonical path (parent traversal bypass, no symlink/worktree escape check).
- **Correction to prior claim (append-only, history not rewritten):** The `ce0ecac` Session-Log and this SSOT’s `Cycle 3 — CANONICAL RUNNER IMPLEMENTATION` entry claimed “실제 retrieval/DB/model/benchmark/latency는 구현되어 있으나 …” and “runner is code-complete”. This was **false** — orchestration was placeholder and the runner was not execution-ready. The inaccurate sentence is preserved in history; this hardening entry appends the correction. Web review **HOLD** found blockers **A/B/C** as above.
- **Scope of this repair (sparse isolated worktree, `codex/retrieval-v2-cycle3-candidate`, no canonical execution):** No real retrieval/DB query/model/embedding load/benchmark/canonical latency measurement/result inspection; `CYCLE3_CANONICAL_EXECUTION=1` not set/used; no holdout/dev plaintext accessed; no `git show/cat-file/checkout/restore/sparse-checkout/parent worktree` recovery of excluded plaintext; no production `ml-service` behavior change (diff 0); no prereg candidate/K/threshold/selection/SQL semantics change (prereg-v1 `18b6c997...` unchanged); no new candidate/K/threshold/rerun rule/result artifact; no history rewrite; existing 16-event real audit log `eval/retrieval-v2/cycle3/audit/events.jsonl` untouched (tests use temp logs only).
- **Fix A — missing import:** `run_cycle3_canonical_dev.py` now imports `assert_rp_is_null` from `cycle3_runner` (`from retrieval_v2.cycle3_runner import ... assert_rp_is_null`). Regression: `test_retrieval_v2_cycle3_canonical_hardening.py::MissingImportRegressionTest` exercises the CLI/orchestrator boundary (canonical paths → grant gate) and would have raised `NameError` on `ce0ecac`.
- **Fix C — path confinement:** Replaced `posixpath.normpath` with `_confine_to_canonical()` — repo-root `resolve()` exact match, explicit `..` rejection (`PurePosixPath.parts` contains `..` → fail-closed), `candidate.relative_to(repo_root)` for symlink/worktree escape, canonical dev (`eval/retrieval-v2/cycle3/dev/evalset.jsonl`) / output (`eval/retrieval-v2/cycle3/canonical-dev/canonical-dev-result.json`) / audit (`eval/retrieval-v2/cycle3/audit/events.jsonl`) must resolve to exactly intended in-repo locations. Regression: `PathConfinementRegressionTest` shows old `posixpath` traversal-bypass vs new reject, plus absolute-outside, backslash, and parent-traversal variants.
- **Fix B — execution-ready orchestration (code exists, NOT executed in this stage):** Added `eval/retrieval_v2/cycle3_runner.py:orchestrate_4way_batch()` + `rank_of_gold`/`compute_metrics_from_ranks`/`validate_complete_result`/`atomic_write_result` and wired the **single 4-way code path** in `run_cycle3_canonical_dev.py:main()` **after** the `CYCLE3_CANONICAL_EXECUTION=1` gate (still gated, not run now). Semantics: baseline + exactly `c3e1 K128/c3e2 K256/c3e3 K512`, same DB/corpus/query/qvec (strip_region once, `embedding_fn` once per case, same qvec for 4), baseline `lexical_overlap_terms(strip_region(raw))` vs candidate `lexical_overlap_terms_rewrite(...)`, bounded vector pre-pool `K` then lexical on `K` only, `FINAL_N=30`, `RERANK=0`, `COSINE_MIN=.78` post-LIMIT (`apply_cosine_filter`), `LEXICAL_BIAS=.01`, `rp NULL` (`assert_rp_is_null`), deterministic tie-break `dist/source/source_id`, paired per-case ranks/metrics (`source_macro R@5`/`net hit@5`/`Youth/Gov24`), `quality_selectable` → `dev_selectable` via predefined warm paired latency (same-process/same-DB/interleaved/warmup-excluded, timed count fixed before inspection) **only for quality-selectable candidates**, `DEV_SELECTABLE` tie-break `net -> macro -> delta -> smaller K`, zero selectable closes without holdout.
- **Injectable/fake dependencies:** `orchestrate_4way_batch(dev_items, embedding_fn, retrieval_fn, latency_measurer)` is pure and takes fakes; CLI/orchestrator end-to-end is testable with **no DB/model/plaintext**. Tests prove reachability, metrics, quality-only latency, tie-break/zero-selectable, audit closure, schema/atomic/rerun denial, failure behavior without touching protected plaintext.
- **Audit lifecycle (future real run, wired but not executed now):** `require_protected_dev_access_grant` → `append_canonical_run_start` → (open dev, verify SHA, load, orchestrate) → `validate_complete_result` → `atomic_write_result` (temp+fsync+rename, existing output rerun fail-closed) → `append_canonical_run_end` (success) + `protected_access_end` (success); failure paths append `run_end(failure)` + `protected_access_end(failure)` and fail-closed (no partial write). Real audit log not appended in this stage (temp logs only).
- **Tests (this repair):** `eval/test_retrieval_v2_cycle3_canonical_hardening.py` (27 tests, pure/fake) + `eval/test_retrieval_v2_cycle3_runner.py` (50) — all prove the above contracts and would have failed on `ce0ecac` (NameError, traversal, placeholder). No dev plaintext `git show`/DB/model required.
- **Files changed in this repair (single atomic commit, no tag):** `eval/retrieval_v2/cycle3_runner.py` (orchestration, metrics, atomic write), `eval/retrieval_v2/run_cycle3_canonical_dev.py` (import, confinement, orchestration wiring, audit lifecycle), `eval/test_retrieval_v2_cycle3_canonical_hardening.py` (new hardening tests), `docs/RETRIEVAL_V2.md` (this section, current state/next gate), `memory/SESSION-LOG.md` (append-only correction). `ml-service` diff 0, `prereg-v1.json` unchanged (`18b6c997...`), `D-003/D-004/D-007/D-008/D-009/D-010/D-011` preserved.
- **Validations (this repair, no canonical execution):** `python -m py_compile` PASS, `pytest eval/test_retrieval_v2_cycle3_canonical_hardening.py` 27 passed, `pytest eval/test_retrieval_v2_cycle3_runner.py` 50 passed, `pytest eval/test_retrieval_v2_cycle3_*` pure/static  ≥83 passed, `git diff --check` PASS, `ml-service` diff 0, existing 16-event audit SHA unchanged, no result artifact created.
- **Current Cycle3 state (post-repair):** Datasets frozen (fresh dev 36 `3791368f...` + fresh holdout 40 `4c631ce7...`, catalog 6-set union 248), pre-reg fixed (3 candidates K128/256/512), **canonical runner is now execution-ready (code path complete) but NOT executed** — `CYCLE3_CANONICAL_EXECUTION` gate still blocks `main()` in this worktree.
- **Next gate (explicit):** **Web independent static review** on this new commit. **Canonical dev batch execution remains forbidden until that Web review PASS.** After PASS, a dedicated canonical execution session (single batch, audit grant, `CYCLE3_CANONICAL_EXECUTION=1`) may run the now execution-ready orchestration exactly once; zero `DEV_SELECTABLE` closes without holdout; holdout evaluation requires separate freeze+review+approval per `D-011` and `prereg` §8.
## Cycle 3 — 9-BLOCKER REPAIR (Web HOLD on 0efb52a) — 2026-09-01 — sparse isolated, no execution

- **Correction to prior claim (append-only, history not rewritten):** The `0efb52a` Session-Log and this SSOT’s `Cycle 3 — CANONICAL RUNNER HARDENING` entry claimed the runner was execution-ready. Web independent static review on `0efb52a` issued **HOLD** with 9 blockers (1-9), not PASS — the runner was not execution-ready. The inaccurate execution-ready sentence in the prior entry is preserved; this section appends the correction.
- **Web HOLD blockers (0efb52a → this repair):** 1) Production embedding parity: `model.encode(stripped)` must be `model.encode([f"query: {q}"], normalize_embeddings=True)[0]` with 6-decimal pgvector string. 2) Same DB/corpus provenance: total policy/chunk + Youth/Gov24 split from same connection, validate 13589/17609 and record. 3) MRR@10 must be 0 beyond rank 10. 4) Audit/result lifecycle: if run_end or protected_access_end fails, no result may remain; mandatory closure not swallowed. 5) One-shot/rerun: durable audit must reject second attempt after prior run_start even if failed before output, plus concurrent race guard. 6) Coverage: true E2E of actual CLI/main path via injection. 7) Grant/token pinning explicit fail-closed. 8) Complete result strict: git SHA, selected only c3e1/c3e2/c3e3/None, latency only for quality+baseline, corpus, metrics consistency. 9) Durable docs correction.
- **Scope of this repair (sparse isolated worktree, `codex/retrieval-v2-cycle3-candidate`, `0efb52a` → new HEAD, no canonical execution):** No real retrieval/DB/model/embedding/benchmark/latency/result artifact; `CYCLE3_CANONICAL_EXECUTION=1` only for injected fake tests; no dev/holdout plaintext via `git show`/`cat-file`/`checkout`/`restore`/`sparse`/`parent worktree`; no production `ml-service` change (diff 0); no prereg/D-003..D-011 change; no new candidate/K/threshold; no result artifact; no history rewrite; real audit log `eval/retrieval-v2/cycle3/audit/events.jsonl` untouched (16 events, SHA `a7be621e433ed3788c97f26081e291cd530dd7e99ceafdeceadd5a1cc478bc9c`).
- **Fix 1 — embedding parity:** `run_cycle3_canonical_dev.py:_real_embedding_fn_factory` now does `model.encode([f"query: {stripped}"], normalize_embeddings=True)[0]` and `format_pgvector` (`"[" + ",".join(f"{x:.6f}" ...) + "]"`).
- **Fix 2 — corpus provenance:** Added `EXPECTED_CORPUS_*` (13589/17609, youth 2631/3083, gov24 10958/14526), `get_corpus_provenance(conn)` (same connection) and `validate_corpus_provenance` in `cycle3_runner.py`; wired in `run_cycle3_canonical_dev:main()` to query same connection, validate, pass to `orchestrate_4way_batch(..., corpus_provenance=...)` and record in `result["corpus_provenance"]`.
- **Fix 3 — MRR@10:** `compute_metrics_from_ranks` now `1 <= r <= 10` else 0; rank-11 regression.
- **Fix 4 — audit/result lifecycle:** Rewrote `main()` success/failure to be fail-closed: if `append_canonical_run_end` or `protected_access_end` fails, result is unlinked and `_fail_closed`; `finally` ensures no result on failure.
- **Fix 5 — one-shot/race:** Added `assert_no_prior_canonical_attempt(log_path)` (durable audit for prior `run_start` with `BATCH_ID`) called before/after grant and before `run_start`; `atomic_write_result` now uses unique tmp `".tmp.{uuid}"` + `os.link` hardlink exclusive + pre-publish re-check.
- **Fix 6 — coverage:** Added `eval/test_retrieval_v2_cycle3_cli_e2e.py` (19 tests) as true E2E of actual CLI/main path via injection (temp root/log/output, fake load/corpus/embed/retrieval/latency) covering success, failure, closure-failure, one-shot, token, traversal. Updated `test_retrieval_v2_cycle3_canonical_hardening.py` to provide fake latency for quality case.
- **Fix 7 — grant/token:** Added CLI `--grant-token`, `token_source` (`cli --grant-token` > `env CYCLE3_GRANT_TOKEN`), 64-hex validation, `expected_event_hash` pinning, fail-closed before plaintext open, code/docs/tests consistent.
- **Fix 8 — result schema/provenance:** `build_result_skeleton` now takes `corpus_provenance`; `validate_complete_result` now strict: git 40-hex + dirty bool, selected only c3e1/c3e2/c3e3/None, latency only for quality+paired baseline, corpus required, metrics/per-case consistency.
- **Docs:** This correction is append-only; prior inaccurate execution-ready sentence not edited. `memory/SESSION-LOG.md` appended with `2026-09-01 — CORRECTION — 0efb52a Web HOLD` entry reflecting 9 blockers.
- **Validations (this repair, no canonical execution):** `python -m py_compile` PASS; `pytest eval/test_retrieval_v2_cycle3_cli_e2e.py` 19 passed; `pytest eval/test_retrieval_v2_cycle3_canonical_hardening.py` 27 passed; `pytest eval/test_retrieval_v2_cycle3_runner.py` 50 passed; `pytest eval/test_retrieval_v2_cycle3_*.py` (audit, fingerprint, prereg, infra_repair, historical_catalog, runner, hardening, cli_e2e) 172 passed; relevant Cycle3 pure/static ≥190 passed; `git diff --check` PASS; `ml-service` diff 0; real audit SHA unchanged; no result artifact.
- **Current Cycle3 state (post-repair):** Datasets frozen (fresh dev 36 `3791368f…` + holdout 40 `4c631ce7…`), pre-reg fixed (3 candidates), **canonical runner is now execution-ready after 9-blocker repair but NOT executed** — `CYCLE3_CANONICAL_EXECUTION` gate still blocks `main()` in this worktree.
- **Next gate (explicit):** **Web independent static review** on this new commit. **Canonical dev batch execution remains forbidden until that Web review PASS.**
## Cycle 3 — LATENCY HOLD REPAIR (Web HOLD on a19b274) — 2026-09-01 — sparse isolated, no execution

- **Correction to prior claim (append-only, history not rewritten):** The `a19b274` Session-Log and this SSOT’s `Cycle 3 — 9-BLOCKER REPAIR` entry claimed the runner was execution-ready after 9-blocker fixes. Web independent review of `a19b274` issued **HOLD for a prereg/D-007 latency methodology drift** — the runner was not methodology-conformant. The inaccurate execution-ready sentence in the prior entry is preserved; this section appends the correction.
- **Web HOLD blocker (a19b274 → this repair):** `docs/RETRIEVAL_V2_CYCLE3_PREREG.md §7` fixes Cycle3 latency methodology to D-007 `same-process/same-DB/interleaved/warmup-excluded`. The canonical D-007 harness `eval/retrieval_v2/run_latency_candidate_gate.py` precomputes `qvec` before warmup/timing, warms every benchmark query once per variant (36/variant), and starts each timed sample before variant-specific lexical term generation + `youth_source_bias`, then includes SQL/fetch + post-LIMIT cosine filter; `5x36=180` timed samples/variant. In `a19b274`, `_real_latency_measurer_factory` instead recomputed `terms_map`/`youth bias`/`qvec` before `t0`, did `random-with-replacement` warmup (`rng.choice`), and `t0` started only immediately before `retrieval_fn`. Pure static reproduction: Cycle3 `run_cycle3_canonical_dev.py` lines ~302 `terms_map`, 303 `yb`, 304 `embedding_fn`, 309 `t0` vs D-007 `run_latency_candidate_gate.py` lines ~385 `t0`, 387 `lexical_terms`, 388 `youth_bias`, 389 `SQL`. This can change the `p95` gate and is execution-blocking (Web HOLD).
- **Scope of this repair (sparse isolated worktree, `codex/retrieval-v2-cycle3-candidate`, `a19b274` → new HEAD, no canonical execution):** No real retrieval/DB/model/embedding/benchmark/latency/result artifact; `CYCLE3_CANONICAL_EXECUTION=1` only for injected fake tests; no dev/holdout plaintext via `git show`/`cat-file`/`checkout`/`restore`/`sparse`/`parent worktree`; no production `ml-service` behavior change (`ml-service` diff 0); no prereg candidate/K/threshold/selection-rule change (prereg-v1 `18b6c997...` unchanged, D-003/D-007/D-011 preserved); no new candidate/K/threshold/rerun rule/result artifact; no history rewrite; real audit log `eval/retrieval-v2/cycle3/audit/events.jsonl` untouched (16 events, SHA `a7be621e433ed3788c97f26081e291cd530dd7e99ceafdeceadd5a1cc478bc9c`).
- **Fix — latency methodology (`eval/retrieval_v2/run_cycle3_canonical_dev.py:_real_latency_measurer_factory` + `eval/retrieval_v2/cycle3_runner.py` stripped helpers):** Rewrote to D-007 methodology while preserving Cycle3 `quality-selectable-only` latency and prereg selection/tie-break: (a) precompute `stripped = strip_region(raw)` + `qvec = embedding_fn(stripped)` outside warmup/timed — exactly 36 strip+qvec calls per batch, reused for all warmup/timed samples (strip + embedding excluded from timed, no re-encode/re-strip); warmup uses `lexical_terms_for_stripped(stripped)` / `youth_bias_for_stripped(stripped)`; (b) warm all 36 dev queries exactly once per measured variant, excluded from timed, interleaved per case in fixed order (baseline first), not `rng.choice` replacement; (c) timed `5 rounds x 36` per measured variant = `180/variant`, interleaved/paired per round (`Random(SHUFFLE_SEED+rnd).shuffle` then for 2 variants `alternating (rnd+q_idx)%2` exactly as D-007, otherwise `sample`), with `t0 = time.perf_counter_ns()` **before** `lexical_terms_for_stripped` + `youth_bias_for_stripped` and including SQL/fetch + `apply_cosine_filter` inside `t0..t1` (`(t1-t0)/1e6` ms); (d) fixed timed count before inspection (`timed_per_variant = len(dev_items)*5`); canonical nearest-rank `p50 = s[ceil(0.5*n)-1]` / `p95 = s[ceil(0.95*n)-1]` (`latency.py`) rounded to 2 decimals for gate (not linear interpolation); same process/DB/corpus/query set, cold/model-load excluded.
- **Tests (this repair, pure/fake, WOULD FAIL on a19b274):** Added `eval/test_retrieval_v2_cycle3_latency_hold_repair.py` (14 tests, pure/fake, no DB/model/plaintext) exercising the **real** latency factory with fakes: `LatencyQvecPrecomputeTest` (embed exactly 36, no re-encode), `LatencyWarmupCoverageTest` (each 36 exactly once per variant, not random), `LatencyTimedCountTest` (180/variant, quality-only), `LatencyInsideTimedBoundaryTest` (lexical/youth/retrieve/filter inside `t0..t1` + static `t0 < lex` + strip not inside + region-bearing strip test + nearest-rank p95), `LatencyQualitySelectableOnlyTest` + `LatencyBaselinePairedTest` (non-quality never measured, baseline paired). Old `a19b274` fails qvec (216), warmup distribution, inside-boundary, strip reprocessing, and p95 interpolation checks.
- **Preserved:** `quality-selectable-only` latency (orchestrator `latency_measurer(quality_ids)` only, non-quality `None`, baseline paired), prereg §7 tie-break (`net hit5 → macro → delta → smaller K`), zero-selectable handling, `FINAL_N=30`, `RERANK=0`, `COSINE_MIN=0.78`, `LEXICAL_BIAS=0.01`, SQL bounded pool semantics, audit lifecycle — unchanged.
- **Files changed in this repair (single atomic commit, no tag):** `eval/retrieval_v2/run_cycle3_canonical_dev.py` (latency factory), `eval/retrieval_v2/cycle3_runner.py` (stripped helpers), `eval/test_retrieval_v2_cycle3_latency_hold_repair.py` (new), `docs/RETRIEVAL_V2.md` (this section), `memory/SESSION-LOG.md` (append-only HOLD correction). `ml-service` diff 0, `prereg-v1.json` unchanged (`18b6c997...`), `D-003/D-004/D-007/D-008/D-009/D-010/D-011` preserved.
- **Validations (this repair, no canonical execution):** `python -m py_compile` PASS; `pytest eval/test_retrieval_v2_cycle3_latency_hold_repair.py` 14 passed; `pytest eval/test_retrieval_v2_cycle3_cli_e2e.py` 19 passed; `pytest eval/test_retrieval_v2_cycle3_canonical_hardening.py` 27 passed; `pytest eval/test_retrieval_v2_cycle3_runner.py` 50 passed; `pytest eval/test_retrieval_v2_cycle3_*` (incl. audit/fingerprint/prereg/infra_repair/historical_catalog/runner/hardening/cli_e2e/latency_hold) 186 passed; `git diff --check` PASS; `ml-service` diff 0; real audit `events.jsonl` SHA `a7be621e...` unchanged (16 events); no `canonical-dev-result.json` artifact; `canonical_output absent` verified; `CYCLE3_CANONICAL_EXECUTION` not set against real dependencies.
- **Current Cycle3 state (post-repair):** Datasets frozen (fresh dev 36 `3791368f…` + holdout 40 `4c631ce7…`, catalog 6-set union 248), pre-reg fixed (3 candidates `c3e1 K128/c3e2 K256/c3e3 K512`), **canonical runner latency harness now matches frozen D-007 methodology (precomputed stripped q + qvec, warmup 36/variant exact, t0 before lexical+youth via stripped helpers, perf_counter_ns, nearest-rank p95, 180/variant interleaved/paired) and is execution-ready but NOT executed** — `CYCLE3_CANONICAL_EXECUTION` gate still blocks `main()` in this worktree.
- **Next gate (explicit):** **Web independent static review** on this new commit. **Canonical dev batch execution remains forbidden until that Web review PASS.** After PASS, a dedicated canonical execution session (single batch, audit grant, `CYCLE3_CANONICAL_EXECUTION=1`) may run the now methodology-conformant orchestration exactly once; zero `DEV_SELECTABLE` closes without holdout; holdout evaluation requires separate freeze+review+approval per `D-011` and `prereg` §8.
## Cycle 3 — LIFECYCLE + OWNERSHIP REPAIR (Web HOLD on 5892762) — 2026-09-01 — sparse isolated, no execution

- **Correction to prior claim (append-only, history not rewritten):** The `5892762` Session-Log and this SSOT's `Cycle 3 — LATENCY HOLD REPAIR` entry claimed the runner was lifecycle-safe after latency fix. Web independent review of `5892762a6e993ea7f6dde7f594906b09ce77e830` issued **HOLD for TWO execution-blocking lifecycle bugs** — runner was not lifecycle-safe. Latency repair itself now passes, but lifecycle still blocks. This section appends correction without rewriting prior entries.
- **Web HOLD blockers (5892762 → this repair):** **BLOCKER 1 — protected grant remains open on failures after grant but before run_start.** `main()` obtains `require_protected_dev_access_grant()` then lines 463-500 can fail for missing `CYCLE3_CANONICAL_EXECUTION`, missing dev path, dev SHA mismatch, second one-shot recheck, or `run_start` append failure, without appending `protected_access_end`. Pure Web repro: valid temp `protected_access_start` + missing execution env => main fails, then `verify_holdout_access_allowed` with same session/token still succeeds (grant remains open). **BLOCKER 2 — failure cleanup can delete output not created by this invocation.** `failure`/`finally` unlinks `out_path` whenever `success=False` even if `result_path is None`, based on false assumption audit one-shot makes it owned. Pure Web repro: after initial output guard, injected fake retrieval simulates another writer publishing canonical output then raises; this invocation's cleanup deletes that foreign output (`FOREIGN_OUTPUT_SURVIVES=False`). Also `os.link`/`O_EXCL` fallback can overwrite after relinquishing exclusivity.
- **Scope of this repair (sparse isolated worktree, `codex/retrieval-v2-cycle3-candidate`, `5892762` → new HEAD, no canonical execution):** No real retrieval/DB/model/embedding/benchmark/latency/result artifact; `CYCLE3_CANONICAL_EXECUTION=1` only for injected fake tests; no dev/holdout plaintext via `git show`/`cat-file`/`checkout`/`restore`/`sparse`/`parent worktree`; no production `ml-service` behavior change (`git diff 5892762 -- ml-service/` diff 0); no prereg candidate/K/threshold/selection-rule/SQL semantics change (prereg-v1 `18b6c997eb71a8cdff36d84ff46b5bbb6b699874ff6d0fccd18636f00268e156` unchanged, D-003/D-007/D-011 preserved); no new candidate/K/threshold; no result artifact (`canonical-dev-result.json` absent); no history rewrite; real audit log `eval/retrieval-v2/cycle3/audit/events.jsonl` untouched (16 events, SHA `a7be621e433ed3788c97f26081e291cd530dd7e99ceafdeceadd5a1cc478bc9c`).
- **Fix BLOCKER 1 — grant lifecycle fail-closed (`eval/retrieval_v2/run_cycle3_canonical_dev.py`):** Moved non-sensitive execution gate `CYCLE3_CANONICAL_EXECUTION` check before `require_protected_dev_access_grant()` (prefer gate before grant). Once grant is successfully acquired, every exit before/at `run_start` now closes that exact dev grant with `protected_access_end` outcome `failure` if canonical access does not proceed — added `_close_grant_on_pre_failure` helper wrapping dev-path, SHA mismatch, second one-shot, `run_start` append failure; if closure itself fails, that closure failure is surfaced (fail-closed). `protected_access_end` exactly once on all post-grant failures/success, `run_start`/`run_end` coherent, actual plaintext open/SHA access remains after verified grant, no reusable open grant on failure. Tests use temp audit only.
- **Fix BLOCKER 2 — ownership-safe cleanup + atomic fallback (`eval/retrieval_v2/cycle3_runner.py:atomic_write_result` + `eval/retrieval_v2/run_cycle3_canonical_dev.py`):** Changed failure/`finally` to be ownership-safe: only remove artifact this invocation can prove it created/published (`result_path is not None`). Removed foreign-delete paths (`else: unlink out_path when result_path is None` and `if not success: unlink out_path`). Preserved atomic no-overwrite and rerun prevention: `atomic_write_result` still validates complete result, fails if `out_path` exists, uses unique `tmp.{uuid}` + `os.link` hardlink exclusive + pre-publish re-check. Rewrote `OSError` fallback (EXDEV/cross-device) to use `os.open(O_CREAT|O_EXCL|O_WRONLY)` + direct payload write + `fsync` + validation, then unlink `tmp`; avoids `tmp.replace` overwrite after relinquishing exclusivity. Ownership tracked via `result_path`.
- **Tests (this repair, pure/fake, WOULD FAIL on 5892762):** Added `eval/test_retrieval_v2_cycle3_runner_audit_lifecycle_repair.py` (14 tests, pure/fake, no DB/model/plaintext, actual `main()` E2E via injection) covering: (a) missing execution env gate-before-grant without leaving reusable acquired grant; (b) missing dev path / SHA mismatch / second one-shot race / `run_start` append failure each closes grant exactly once with `failure` and denies subsequent `verify`; (c) successful path closes exactly once (`run_start`/`run_end`/`protected_access_end` once, linkage hashes, order); (d) retrieval failure closes once + no own result; (e) foreign output after initial guard survives failing contender (ownership-safe); (f) atomic publish no-overwrite race/fallback ownership — preexisting blocks, concurrent race after initial guard blocked, fallback exclusive-create no-overwrite, foreign not deleted. Old `5892762` fails B1 (grant open), B2 (foreign deleted), fallback overwrite.
- **Adjacent review (narrow boundary):** Verified `protected_access_end` exactly once on all post-grant failures/success, `run_start`/`run_end` coherent (no `run_end` before `run_start` on pre-run failures), no result survives own failed closure (owned result removed on `run_end`/`protected_end` failure), foreign output never deleted, durable one-shot still rejects later attempts after `run_start`, and complete result/audit linkage intact (`provenance` grant/run_start hashes, `corpus_provenance`, `git` provenance, metrics consistency). No broadening beyond audit/result publication/rerun lifecycle.
- **Files changed in this repair (single atomic commit, no tag):** `eval/retrieval_v2/cycle3_runner.py` (atomic fallback), `eval/retrieval_v2/run_cycle3_canonical_dev.py` (grant lifecycle + ownership cleanup), `eval/test_retrieval_v2_cycle3_runner_audit_lifecycle_repair.py` (new), `docs/RETRIEVAL_V2.md` (this section), `memory/SESSION-LOG.md` (append-only HOLD correction). `ml-service` diff 0, `prereg-v1.json` unchanged (`18b6c997...`), `D-003/D-004/D-007/D-008/D-009/D-010/D-011` preserved, `ROOT` remains `Muse Spark 1.2 Contributor / xhigh`.
- **Validations (this repair, no canonical execution):** `python -m py_compile` PASS; `pytest eval/test_retrieval_v2_cycle3_runner_audit_lifecycle_repair.py` 14 passed; `pytest eval/test_retrieval_v2_cycle3_canonical_hardening.py` 27 passed; `pytest eval/test_retrieval_v2_cycle3_cli_e2e.py` 19 passed; `pytest eval/test_retrieval_v2_cycle3_latency_hold_repair.py` 14 passed; `pytest eval/test_retrieval_v2_cycle3_runner.py` 50 passed; `pytest eval/test_retrieval_v2_cycle3_*` pure/static 200+ passed; `git diff --check` PASS; `git diff 5892762 -- ml-service/` diff 0; `git diff --check` from dev-freeze base PASS; real audit `eval/retrieval-v2/cycle3/audit/events.jsonl` SHA `a7be621e433ed3788c97f26081e291cd530dd7e99ceafdeceadd5a1cc478bc9c` unchanged (16 events, count 16); `canonical-dev-result.json` absent; `holdout` plaintext absent; `CYCLE3_CANONICAL_EXECUTION` not set against real deps; prohibited execution count 0.
- **Current Cycle3 state (post-repair):** Datasets frozen (fresh dev 36 `3791368f…` + holdout 40 `4c631ce7…`, catalog 6-set union 248), pre-reg fixed (3 candidates `c3e1 K128/c3e2 K256/c3e3 K512`), **canonical runner now lifecycle-safe (grant lifecycle fail-closed, ownership-safe cleanup, atomic no-overwrite) and latency-conformant, but NOT executed** — `CYCLE3_CANONICAL_EXECUTION` gate still blocks `main()` in this worktree.
- **Next gate (explicit):** **Web independent static review** on this new commit. **Canonical dev batch execution remains forbidden until that Web review PASS.** After PASS, a dedicated canonical execution session (single batch, audit grant, `CYCLE3_CANONICAL_EXECUTION=1`) may run the now lifecycle-safe orchestration exactly once; zero `DEV_SELECTABLE` closes without holdout; holdout evaluation requires separate freeze+review+approval per `D-011` and `prereg` §8.

  ## Known limitations (non-blocking)
