# Retrieval v3 D-077 dev-v2 generation-v7 strategy — author-isolation pre-result freeze

Status: D-077 NEW LOGICAL STAGE (user-authorized continuation). Plaintext-free: SHAs, counts, timestamps, filenames, structural facts only — no query text, no labels, no id mappings.
Stage identity: D-077 creates a generation-v7 PRE-RESULT freeze ONLY. No source-truth snapshot, no candidate generation, no A/B/C, no selector in D-077.

## 1. Reconciled base (this record phase, actual repo/Git/remote/SSOT wins — observed)

- Branch `codex/retrieval-v3-user-search-quality` HEAD `60f726b14d05243134a537e4601a162c6056c200` clean, local = upstream = direct remote identical (`git ls-remote` identical for this branch).
- `git diff --check` PASS; `git diff HEAD -- ml-service/` 0; `git diff d12aba9..HEAD` touches only the two D-076 records + memory (4 files, 78 insertions), so the frozen six remain byte-identical by clean-tree inheritance from the D-076 verification (prereg `78420186…` + plan-v4 `a25d9c48…` + safe-action `c512fb56…` + policy-v2 `6fee9ec2…` direct-SHA verified at D-076; link-V2 `f028ce46…` + cost-V1 `5891b0ba…` via clean pinned HEAD; prereg + cost re-verified by direct SHA in this stage).
- Audit `eval/retrieval-v3/audit/events.jsonl` 4 events SHA `90cfb54df614bc59e01551943436fdafc3cd5cac121b071742acbb2fb604c506` byte-identical (D-068 open grant/run untouched).
- Canonical v3 result absent; `eval/retrieval-v3/dev/` + `holdout/` absent on main; no dev-v2 branch/tag/worktree created or touched; protected v3 dev/holdout freeze branches + worktrees pre-existing, untouched.
- Durable inputs read in this stage: D-075-SC correction doc + `D-075` DECISIONS blocks, D-076 closure doc + `D-076` block, D-076-CORR doc + `D-076-CORR` block, frozen v6r1 `GENERATION_PLAN.json` (22314 bytes, SHA `9bc432648576189affb9333aa5dfaaf2d1f58b387da56f02676aaf01b073cf64`), `RUBRIC.json` (3334 bytes, SHA `6e7b0cd97b54c8c001843338da8ac39ea99827344e81babadfb20194197bdb78`), `PLAN_LOCK.json` (1378 bytes, SHA `c7cbd156d9583089f9b855ebc89e314a8ee2580572e501409494f964746b1b3f`) — all three re-verified byte-identical before use. D-076 fingerprint metadata only: `input/failed_d076_query_fingerprints.json` SHA `3feaab4dd3811d75321663824f9b89146df8a1236b508ab12725b1bfe36818f0`, count/unique 365, plus `input/D076_CONTRACT_INVALID_SUMMARY.json` SHA `0279c69a3e438d8c2ab5eef40a33ac82dc9cea6786a554d69891f84ce3bb0b5b` aggregate SHAs/counts.
- Explicitly NOT read in this stage: D-076 semantic rows, `source_truth.jsonl`, `candidates_merged.json`, `author_chunks`, `anchors*.json`, `C:/tmp/repair5.txt` bytes, or any protected old plaintext. No `git cat-file` / `git show` / `checkout` / `restore` / sparse / worktree invocation in this stage (count 0).
- OMP `18.1.5`; root session model `opencode-go/muse-spark-1.3-contributor:xhigh` (runtime context; model config never changed); no project override (`opencode.json`/`.opencode.json` absent); no child-agent fallback through Phase B.
- No protected plaintext recovery, no audit append/run/result/benchmark/retrieval/ranking/model/latency/HTTP, no candidate tuning/B, no ml-service change, no protected branch/tag/worktree/import, no history rewrite in this stage.

## 2. Standing (unchanged)

- D-076 disposition is CONTRACT_INVALID_GENERATION per D-076 + D-076-CORR. Its 360 rows + 5 coordinator-overwritten blocks are failed diagnostic evidence only and NEVER reusable/relabelable/supplemented. The 365 fingerprints are failure-exclusion evidence only. D-076 selector counts MUST NOT tune quotas/reserve.
- D-074 canonical disposition is CONTRACT_INVALID_GENERATION per D-074-SC. Its 360/A/B/C/selector are failed diagnostic evidence only and NEVER reusable/relabelable/supplemented. D-074 selector counts MUST NOT tune quotas/reserve.
- The D-073 paired-grounded ambiguous authoring contract was frozen pre-D074 and remains the intended authoring contract; kept unchanged.
- Final evaluation contract and 18 candidate configs/gates unchanged.

## 3. V7 identity and unchanged final contract

- New private builder only: `C:\Users\joji\Documents\programming\bc-v3-dev-v2-builder-20260905-v7`. Never D-070/D-071/D-072/v5/v6/v6r1 directories.
- New identity: plan_version `retrieval-v3-dev-generation-v7`; seed `benefit-compass-retrieval-v3-dev-v2-generation-v7-2026-09-05` (new builder only); candidate IDs `v3g7-001..v3g7-360`.
- Final contract EXACTLY unchanged: total 180; strata 21/25/21/25/18/20/23/27; headline first-six 130; safety ambiguous 23 + unsupported 27 = 50; location exact 54 at 6/7/6/8/5/6/7/9. 18 candidate configs/gates unchanged.
- Reserve remains uniform 2.0x, exactly 360 slots 42/50/42/50/36/40/46/54 and location 108 at 12/14/12/16/10/12/14/18. Do NOT inflate or target reserve from D-074/D-076 diagnostics. Symmetric construction capacity only; no gate change.
- NO target/reserve change from D074/D076 diagnostics.

## 4. V7 authoring, annotation, rubric, selector (semantics unchanged; exclusion gate +1)

- All eight authoring contracts byte-semantically identical to v6r1, including paired-grounded ambiguous (two distinct snapshot anchors, one omitted dimension, shared frame, two completions plus distinguishing evidence; location-bearing ambiguous shares explicit location with non-location omitted dimension). Verified: `authoring_contracts` object identical. No semantic challenger/prefilter.
- Neutral rubric semantic definitions byte-semantically identical to v6r1 (`dimensions` identical; diff keys are `plan_version` + `rubric_version` only, now `retrieval-v3-dev-generation-v7-rubric-v1`). No quotas/intended labels in rubric.
- A/B genuine independent ALL 360, C EVERY 360, agreement method (query_dim_any/gold_any/full_agreement plus task-local per-gold union+ABSENT diagnostic) and exact lexicographically-smallest selector algorithm unchanged (verified identical; selector `constraints` differ by exactly one line: query-overlap gate gains `/failed-D076-365`; gold overlap stays dev-v1/holdout/history-only).
- Query overlap 0 vs dev-v1/holdout/history + failed D070/D071/D072/D074/D076 (EIGHT sets); gold overlap 0 vs canonical dev-v1/holdout/history only. Selected-only global gold uniqueness. Source snapshot taken only in later Phase C after Web/user continuation. Plan/rubric/lock immutable after freeze; infeasible/contract-invalid => STOP, no supplement/relabel/retune.

## 5. V7 author filesystem-confinement contract (new isolation mechanics only; annotation semantics unchanged)

Durable claim is `two disjoint author staging roots + verified session cwd confinement + frozen mechanics + coordinator-never-authors + same-author repair + pre-A/B hard transcript audit`, not stronger. No OS-sandbox claim.

1) BEFORE authors start, coordinator freezes + SHA-pins all author helper/validator/merge mechanics (see §7): `author_brief.md`, `search_snapshot.py`, `check_anchor.py` (EIGHT sets incl failed-D076-365), `validate_pool.py` (candidate IDs + frozen fail codes only), `merge_chunks.py`, `make_anchors.py`, packet/agreement/C/selector scripts. No validator/helper/merge patching after authoring starts.
2) Coordinator materializes TWO independent random-UUID author staging roots under a neutral private temp parent outside the builder. Author-1 root: role-specific 180-slot packet (positions 1..180) + `source_truth.jsonl` copy + `RUBRIC.json` + author brief + search/check helper + fingerprint-only exclusions + own `out/`. Author-2 root: positions 181..360 + same role-local set + own `out/`. No counterpart packet/output, no keymap/candidates/plan/ledgers, no coordinator files. Prompts contain only role + own staging-root path/local filenames — never builder/repo/other-author/old-builder paths.
3) Authors MUST be launched as genuine top-level Paseo agents (exactly TWO, no generic OMP `task`-child sharding, no subagents, no descendants) via non-GUI CLI `paseo run --cwd <that author's staging root> --provider omp/opencode-go/muse-spark-1.3-contributor --thinking xhigh --mode full` (or byte-equivalent). Coordinator records each author Paseo agent ID + inspect/session evidence showing ACTUAL cwd == staging root, model, fallback flag; mismatch => CONTRACT_INVALID_GENERATION.
4) Mechanical validation (frozen `validate_pool.py`) may ONLY emit candidate IDs + frozen fail codes. Coordinator MUST NEVER author or rewrite `query_text`/ledger semantic content and MUST NEVER patch validators based on failures after authoring starts. Repairs go via `paseo send` to the SAME top-level author agent with IDs + codes only and no suggested replacement text; only that author edits its own output.
5) Coordinator may mechanically copy/hash/merge frozen author bytes after PASS (frozen `merge_chunks.py`) — no semantic edit, no relabeling.
6) BEFORE A/B, hard transcript audit: exactly 2 author agents with no descendants; cwd confinement + effective-path PASS for every author tool call (explicit cwd inside root; relative resolves against pinned root cwd; absolute stays under root; no `..` escape, no parent/system/OMP-session/counterpart/builder/old-builder access); coordinator transcript contains no semantic per-row replacement/write to author outputs. Violation => CONTRACT_INVALID_GENERATION.
7) Only after the author audit PASSES may coordinator proceed to A/B staging. V6r1 reviewer/C independent `--cwd` confinement / no-pre-review-keymap / effective-path gates retained unchanged (plan `reviewer_filesystem_confinement` object identical to v6r1).

## 6. Plan lock truthful flags

- `source_truth_content_read_for_this_plan=false`; `v6_source_truth_content_read=false`; `v6r1_source_truth_content_read_in_this_stage=false` (v6r1 source truth was read only inside the failed D-076 execution, never in this freeze).
- `d070/d071/d072/d074/d076_semantic_rows_read_for_plan=false` (D-076 inputs used: fingerprint-file SHA + count/unique 365 + closure-summary aggregate SHAs/counts only; no `source_truth`/`candidates`/`repair5` bytes read).
- `protected_old_plaintext_read=false`; `git_object_scan_cat-file_show_checkout_restore_sparse_worktree=0`.
- `d074_d076_fingerprint_inputs_carried_byte_identical_from_v6r1=true`; v7 exclusion manifest SHA `ae5fd85001ea547bd4f14e343825017cd6cec9989e95a20fe5e9586ec1e71cae`; `author_mechanics_frozen_before_authors=true` with per-file SHAs in lock.

## 7. Phase B freeze record (frozen BEFORE source-truth content; immutable afterward)

- Builder: `C:\Users\joji\Documents\programming\bc-v3-dev-v2-builder-20260905-v7` (new directory; never D-070/D-071/D-072/v5/v6/v6r1).
- Exclusions (`carry_exclusions_v7.py`): 8 fingerprint files carried byte-identically from v6r1 `input/` — failed_d070 `0acc6f27` (273), failed_d071 `3a037d98` (273), failed_d072 `ff3f65d6` (360), failed_d074 `fde76331` (360), failed_d076 `3feaab4d` (365/365), dev-v1 `57716c6a` (180q/228g), holdout `3463a8a1` (250q/212g), history `42e8534d` (248q/248g) — plus carried `D076_CONTRACT_INVALID_SUMMARY.json` `0279c69a` (aggregate provenance reference). No old plaintext printed/stored outside the v6r1 builder; no D-074/D-076 labels/golds/ledgers/mappings/keymaps read. New v7 manifest `input/EXCLUSION_INPUTS.json` SHA `ae5fd85001ea547bd4f14e343825017cd6cec9989e95a20fe5e9586ec1e71cae` (2065 bytes; EIGHT gates incl `failed-D076-365`, required overlap 0).
- Frozen mechanics (SHA-pinned in plan `author_isolation.mechanics_shas` + lock, all inside the v7 builder): `search_snapshot.py` 1698 bytes `10ef37a1…`, `check_anchor.py` 3060 `afa306d8…` (EIGHT sets), `validate_pool.py` 13970 `86d73e9e…` (EIGHT-set gate; fail records are `{candidate_id, code}` only), `merge_chunks.py` 1465 `e5eb0271…`, `make_anchors.py` 2110 `208cbea9…` (slots `v3g7-001..360`, Author-1 1..180 / Author-2 181..360), `build_packets_ab.py` 3455 `4a101f25…`, `build_packets_c.py` 3216 `141c2958…`, `merge_raw_ab.py` 2198 `e92e15e9…`, `build_agreement.py` 6158 `7e8764e6…`, `merge_c.py` 4547 `0d092658…`, `reconstruct_keymaps.py` 2741 `abf4c11d…`, `run_selector.py` 15615 `46e7a1bc…` (query gate + D076), `take_snapshot.py` 3327 `e651ad8d…`, `author_brief.md` 9246 `7283fb01…` (own-root `$OWN_ROOT` confinement; prompts carry no absolute builder/repo/counterpart paths; repair via same-author `paseo send` with IDs + codes only), `reviewer_brief.md` 4245 `274817d8…`, `c_brief.md` 3026 `e2e62156…`, `carry_exclusions_v7.py` 2230 `2dd6a042…`, `freeze_plan_v7.py` 2138 `ee97090b…`.
- Frozen (`freeze_plan_v7.py`, canonical single-line bytes + LF): `GENERATION_PLAN.json` 28766 bytes SHA `c8adfcf2c1cb86e622c7fa4033b7dd72b63de42494de332261f3a489c770d1da` (version `retrieval-v3-dev-generation-v7`, seed `benefit-compass-retrieval-v3-dev-v2-generation-v7-2026-09-05`, candidate IDs `v3g7-001..v3g7-360`, reserve 42/50/42/50/36/40/46/54=360 + location 12/14/12/16/10/12/14/18=108, paired-grounded ambiguous contract unchanged, seven other contracts unchanged, A/B-all-360 + C-every-360, agreement scope + task-local per-gold method unchanged, exact lexicographically-smallest selector + D076 query-gate added, author-isolation contract added with mechanics SHAs, reviewer/C confinement identical to v6r1, keymap reconstruction unchanged, `stage_forbidden` pins the D-077 git-object-scan ban); `RUBRIC.json` 3330 bytes SHA `597aa53a39c69cfb9409a339ebe8f79f15e07f635899d4d24203289ea46de492` (semantics identical to v6r1 `6e7b0cd9`, version-only change); `PLAN_LOCK.json` 3332 bytes SHA `1db0687e0eb5321a64236991139144440e2f34678b91e91bcce80103afd5d140` (source_truth false, v6/v6r1-not-read-in-this-stage false, d070/d071/d072/d074/d076 semantic false, protected_old false, git-object-scan 0, D074/D076 carried recorded, mechanics SHA-pinned). V6 HOLD + v6r1 CONTRACT_INVALID bytes never modified, remain evidence.
- Provenance (actual, observed): OMP `18.1.5`; root session model `opencode-go/muse-spark-1.3-contributor:xhigh` (runtime context; model config never changed); no project model override (no `opencode.json`/`.opencode.json`); no child agents spawned through Phase B; no fallback model used. Phase C author sessions will be recorded with their model/session evidence at spawn time.
- Gate: this record is committed+pushed and remote-verified BEFORE any Phase C source-truth content read. Post-freeze rule in force: NEVER mutate plan/rubric/lock; infeasible/contract-invalid -> STOP/HOLD, no supplement/relabel/retune/recycle.

## 8. End state and STOP

- Main contains only plaintext-free D-077 strategy/freeze records. Private v7 builder contains exclusions + scripts + plan/rubric/lock only; `source_truth.jsonl` ABSENT; candidates ABSENT; author/reviewer staging roots NOT created yet.
- Verify clean local=upstream=direct remote, diff-check PASS, ml-service 0, frozen six/audit exact, v6/v6r1 bytes preserved, result/dev/holdout absent, no dev-v2 protected refs.
- STOP for Web independent review. Phase C generation requires Web review/user continuation and is NOT authorized in D-077.

## 9. Forbidden counts (this stage)

Forbidden counts all 0: source-truth snapshot, candidate generation, A/B/C, selector, benchmark/retrieval/ranking/latency/HTTP/model-encode, D068 retry/audit append/result, protected plaintext/recovery, `git cat-file` / `git show` / `checkout` / `restore` / sparse / worktree protected-data/object scanning, protected branch/tag/worktree/import, ml-service change, history rewrite, D074/D076 row reuse (fingerprint-only exclusion excepted).
