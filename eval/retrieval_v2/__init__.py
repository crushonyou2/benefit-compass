"""Retrieval v2 evaluation scaffolding — D-007 contract.

This package is eval-only and never touches production retrieval
(`ml-service/app.py`, `source_ranking.py`) or canonical artifacts
(`eval/canonical_*.json`).

Roles: dev vs final-holdout are explicit metadata, not inferred.
"""
